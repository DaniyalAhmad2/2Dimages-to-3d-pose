# Auditor notes / negative results

## auditor (categories ['calibration-extrinsics', 'head', 'live-edit', 'robustness', 'testing', 'ui-feedback']) baseline_numbers: {"pytest":{"collected":189,"passed":183,"skipped":6,"failed":0,"seconds":4.88,"exit":0,"panoptic_validators_collected":0},"gauge_on_client_take_shipped_rig":{"source_used_by_code":"f.pose3d (model.py:187)","gauge_pct_median":52.3,"gauge_pct_min":41.0,"gauge_pct_max":67.6,"frames_green":0,"frames_amber":0,"frames_red":26,"timeline_green":0,"timeline_amber":11,"timeline_red":15,"per_joint_err_px_median":3.96,"joint_dots_green":48,"joint_dots_amber":58,"joint_dots_red":282},"gauge_if_it_used_fitted3d":{"gauge_pct_median":6.2,"per_joint_err_px_median":19.7,"timeline_green":0,"timeline_amber":1,"timeline_red":25},"gauge_vs_smoother":{"reads_pose3d_so_identical":52.3,"fitted_smoothed":6.2,"fitted_unsmoothed":39.8,"displacement_mm_mean":6.07},"wrong_rig_sweep":[{"perturbation":"baseline","gauge_pct":52.3,"reproj_px":3.96,"epi_px":4.91,"bone_cv_pct":5.27,"span_m":0.1277,"shift_mm":0.0},{"perturbation":"right_R_rot_5deg","gauge_pct":51.4,"reproj_px":4.16,"epi_px":5.24,"bone_cv_pct":5.73,"span_m":0.1246,"shift_mm":6.93},{"perturbation":"right_R_rot_15deg","gauge_pct":49.4,"reproj_px":4.29,"epi_px":5.33,"bone_cv_pct":7.42,"span_m":0.1214,"shift_mm":19.13},{"perturbation":"focal_2833","gauge_pct":2.8,"reproj_px":58.19,"epi_px":81.49,"bone_cv_pct":10.75,"span_m":0.144,"shift_mm":25.27},{"perturbation":"focal_6000","gauge_pct":5.4,"reproj_px":49.04,"epi_px":68.91,"bone_cv_pct":10.88,"span_m":0.0779,"shift_mm":69.37},{"perturbation":"marker_0.05_to_0.10m","gauge_pct":52.3,"reproj_px":3.96,"epi_px":4.91,"bone_cv_pct":5.27,"span_m":0.2553,"shift_mm":274.76,"note":"every quality metric bit-identical to baseline"}],"destructive_cross_view_gate":{"dropped_by_bad_rig_f2833":209,"nan_obs_after_bad_import":211,"nan_obs_after_recalibrate_with_good_rig":211,"nan_obs_if_good_rig_from_start":2,"joints_lost_permanently":209,"missing_3d_joints_after_vs_clean":[91,0],"recovered":false},"edit_path_vs_recompute_path":{"step_mm_median":4.92,"step_mm_p90":12.77,"step_mm_max":25.65,"body_height_m":0.1178,"step_pct_height_median":4.2,"step_pct_height_p90":10.8,"step_pct_height_max":21.8},"resolution_asymmetry":{"left_size":[3072,4080],"left_focal":4080.0,"left_reproj_px":5.11,"left_pct_of_diag":0.100,"left_gauge_pct":42.7,"right_size":[1536,2048],"right_focal":2048.0,"right_reproj_px":2.78,"right_pct_of_diag":0.109,"right_gauge_pct":62.9,"epipolar_threshold_px":71.5,"thr_pct_left_diag":1.40,"thr_pct_right_diag":2.79},"panoptic_reference_gauge":{"image_size":[1920,1080],"left_reproj_px":5.31,"right_reproj_px":4.34,"gauge_pct_median":44.7,"band":"red","epipolar_threshold_px":30.8},"check_rig_shipped_warnings":3,"world_up_tilt_deg":89.3,"looks_assumed":{"left":true,"right":true},"intrinsics_source_field_loaded":{"left":"measured","right":"measured","actual":"assumed"},"intrinsics_rms":{"left":0.0,"right":0.0}}

NEGATIVE: REFUTED / NULL RESULTS — do not re-open these:

1. H2 as literally worded ('the signals can read GREEN while the 3D is wrong') is REFUTED in the green direction on this take. Under the shipped rig the gauge reads 52.3% median (min 41.0, max 67.6) and lands in the RED 'Low' band on 26/26 frames; the timeline is 0 green / 11 amber / 15 red. The ground-truth-calibrated Panoptic reference reads 44.7% — also red. The correct statement is the opposite failure mode: the signal is SATURATED red and carries no information. It also barely moves when the rig is wrong (52.3 -> 49.4% for a 15 deg extrinsic rotation that shifts every joint 19.13 mm), and is bit-identical (52.3% both ways) for a marker-size error that doubles the world. Frame the finding as 'uninformative and rig-blind', not 'falsely green'.

2. H5's 'no report of which tag/frame calibrated' is REFUTED at the message level: `resolve_calibration` builds "Calibration estimated from ArUco marker {tid} (frame {frame_id})" at pose3d/calib/resolve.py:148-149 and the import dialog shows it (import_dialog.py:225). What is true is that it is never persisted and never shown again — see finding 9.

3. H5's implied 'check_rig stays silent on the client rig' is REFUTED. `check_rig` fires three warnings on the shipped rig: world up 89.3 deg off vertical, both cameras' intrinsics assumed from image size, and a resolution mismatch (left 3072x4080 vs right 1536x2048). The client has been told the intrinsics are guessed. The defect is that the warnings carry no numbers and no action, not that they are absent.

4. H4's 'io_project round-trip loses fitted3d / head2d / head3d / corrected' is REFUTED. `save_project`/`load_project` (io_project.py:102-161) write and restore all of them NaN-aware, and this is tested (test_io_project.py:28-55, test_head_keypoints.py:45-58 and :61-79 for legacy files). The one real persistence defect found is `Intrinsics.load` defaulting `source='measured'` for legacy files (intrinsics.py:58), which contradicts its own comment; it is cosmetic today because `looks_assumed` re-derives the answer from K.

5. `validate_cross_view` re-run on the STORED client 2D drops 0 further observations (390 obs, 0/388 pairs over the 71.5 px gate, epipolar median 4.91 px), so the gate is inert on this shipped project. The destructive-loss finding (finding 2) is about what a wrong rig does, demonstrated by simulation with f=2833 (209 dropped, 0 recoverable); it is NOT a claim that this take was already damaged — the original detections are gone, so that cannot be established either way.

6. The test suite is fully green and fast: 189 collected, 183 passed, 6 skipped, 4.88 s, exit code 0, no flakiness, no errors. There is no failing or broken test to fix; the problem is what it does not measure.

7. Panoptic 'validate' scripts do exist and are usable, they are simply not collected (pytest reports 'no tests collected' for tests/validate_panoptic_geometry.py because pyproject sets only testpaths=['tests'] and the default python_files=test_*.py applies). Renaming is sufficient; no rewrite needed.

NOTES: SURPRISE THAT CONTRADICTS THE BRIEFING CONTEXT: the two cameras are NOT both 3072x4080 with K=[[4080,0,1536],[0,4080,2040]]. workspace/pose3d_projects/Imported_Session/calibration/right_intrinsics.json holds K=[[2048,0,768],[0,2048,1024]], image_size [1536,2048], and the right images on disk are genuinely 1536x2048 (PIL-verified on right_0001.jpg; left_0001.jpg is 3072x4080). Both are still f = max(w,h) guesses, so `looks_assumed` is True for both and `check_rig` emits a resolution-mismatch warning. Other auditors quoting "f=4080 for both cameras" from the shared context should re-check; anything that assumes a symmetric rig (a shared epipolar threshold, an averaged pixel error, a single EXIF focal correction) is wrong on this take.

CODE MAP FOR THE SYNTHESIS (file:line, all read in full):
- gauge computation: pose3d/ui/model.py:180-191 (`_accuracy`, uses f.pose3d, nanmean over cameras)
- percentage/banding: pose3d/ui/panels.py:33-61 (`accuracy_pct` exp(-e/6.0); ACC_HIGH=85, ACC_MED=70)
- timeline bands: pose3d/ui/main_window.py:479-485 (worst < 5 green, < 12 amber, else red)
- joint dot colour + tooltip: pose3d/ui/camera_view.py:200-219
- what the 3D view renders: pose3d/ui/main_window.py:450 (f.fitted3d, f.head3d); export at :355
- destructive gate: pose3d/pipeline.py:100-101; threshold pipeline.py:57-60 (LEFT only)
- fit fallback + print: pose3d/pipeline.py:167-180; callers pipeline.py:193, model.py:55, import_dialog.py:214 (none bind the return)
- import's private detection loop: pose3d/ui/import_dialog.py:200-206 vs pose3d/pipeline.py:38-46
- marker-size spinbox: pose3d/ui/import_dialog.py:106-108 (range 0.005-2.0, default 0.05, no validation)
- rig persistence: pose3d/calib/resolve.py:36-49 (`save_rig`, R/t only)
- calibration message built and discarded: pose3d/calib/resolve.py:148-153, shown at import_dialog.py:225
- live-edit refit: pose3d/ui/model.py:157-171; bone-length cache model.py:173-178
- silent excepts on the pose->pixels path: pose3d/ui/view3d.py:99, pose3d/ui/view3d.py:205, pose3d/app.py:95
- intrinsics source default: pose3d/calib/intrinsics.py:56-58 (comment says 'always the image-size guess', code defaults 'measured')

FULL `except` INVENTORY under pose3d/ (12 blanket `except Exception`): selftest.py:127,226 (reported); app.py:95 (silent, finding 10), app.py:143 (inside the excepthook itself); model.py:162 (emits statusMessage); pipeline.py:172 (print only, finding 7); blender_job.py:128,334,655 and blender_export.py:48,52,170 (export path, out of my scope but each swallows a Blender-side failure); import_dialog.py:227 and main_window.py:295 (both raise a QMessageBox — correct); main_window.py:33,50 (progress-line parsing / worker, correct); view3d.py:99,205 (silent, finding 10). Narrow-scope catches (OSError/ValueError/TypeError/ImportError) at runtime.py:62,65, filedialog.py:62,67, io_project.py:70, intrinsics.py:91,95, character.py:240, models.py:101 are all appropriate.

HOW TO REPRODUCE MY NUMBERS:
  cd /media/athena/hd3/Projects/pose3d-tool && MPLBACKEND=Agg .venv/bin/python /tmp/claude-1000/-media-athena-hd3-Projects-pose3d-tool/0fcb8c9e-52c1-483d-9e0a-80896b078fa5/scratchpad/find-tests-ui/ui_signals.py
  cd /media/athena/hd3/Projects/pose3d-tool && MPLBACKEND=Agg .venv/bin/python /tmp/claude-1000/-media-athena-hd3-Projects-pose3d-tool/0fcb8c9e-52c1-483d-9e0a-80896b078fa5/scratchpad/find-tests-ui/scale_blind.py
  cd /media/athena/hd3/Projects/pose3d-tool && .venv/bin/python -m pytest -q -p no:randomly
Both scripts import the baseline harness (scratchpad/baseline/metrics.py) so the numbers stay comparable, and copy `accuracy_pct`/`acc_band`/`_accuracy`/`_refresh_timeline_status` verbatim rather than importing Qt.

RANKING RATIONALE: findings 1-2 are ranked above 3-4 because they explain why the client cannot see or fix the problem at all; finding 3 (scale) is the one most likely to be the client's literal complaint about the EXPORT rather than the view, but I could not verify the physical tag size, so its confidence sits at 0.90 for the mechanism and roughly 0.5 for 'this take is actually mis-scaled'. Findings 5-7 are concrete divergences that produce visible per-frame wrongness. Finding 8 is the reason none of the others were caught.

Nothing in the repository was modified. All outputs are under /tmp/claude-1000/-media-athena-hd3-Projects-pose3d-tool/0fcb8c9e-52c1-483d-9e0a-80896b078fa5/scratchpad/find-tests-ui/ (ui_signals.py, ui_signals.json, scale_blind.py, scale_blind.json, pytest_run.txt, pytest.log).


## auditor (categories ['bone-fit', 'live-edit', 'smoothing', 'ui-feedback']) baseline_numbers: {"take":{"path":"workspace/pose3d_projects/Imported_Session","frames":26,"height_m":0.1178,"fitted3d_equals_refit_smooth_max_m":0.0},"H1_smoothing_disp_mm":{"median":4.92,"mean":6.07,"p90":12.77,"max":25.65,"median_pct_height":4.18,"p90_pct_height":10.84,"max_pct_height":21.78,"worst":["RIGHT_ANKLE","0014",25.65],"frac_over_5pct_height":0.41,"frac_over_10pct_height":0.116,"top_joints_median_mm":{"RIGHT_ANKLE":9.63,"LEFT_ANKLE":8.28,"RIGHT_KNEE":6.91,"LEFT_WRIST":6.91,"HEAD":5.14,"RIGHT_ELBOW":5.09},"top_frames_median_mm":{"0020":14.3,"0014":9.08,"0013":8.78,"0012":7.15}},"H1_lag_direction":{"cos_disp_vs_motion_median":-0.976,"frac_negative":0.954,"n":371,"backward_component_median_mm":4.86,"of_total_median_mm":4.92},"H1_motion":{"interframe_raw_median_mm":9.36,"interframe_raw_p90_mm":26.83,"pct_height":7.95,"ema_lag_frames":0.667,"predicted_lag_mm":6.24},"H1_alpha_sweep":{"1.0":{"bone_cv_med_pct":0.14,"bone_cv_max_pct":0.37,"disp_mean_mm":0.0,"reproj_px":[7.38,4.01]},"0.9":{"bone_cv_med_pct":0.44,"bone_cv_max_pct":3.58,"disp_mean_mm":1.25,"reproj_px":[9.25,5.91]},"0.8":{"bone_cv_med_pct":0.69,"bone_cv_max_pct":3.15,"disp_mean_mm":2.62,"reproj_px":[11.89,8.96]},"0.6_shipped":{"bone_cv_med_pct":1.04,"bone_cv_max_pct":6.16,"disp_mean_mm":6.07,"reproj_px":[21.65,17.18]},"0.4":{"bone_cv_med_pct":1.3,"bone_cv_max_pct":11.33,"disp_mean_mm":11.41,"reproj_px":[40.54,33.78]}},"H1_bone_direction_rotation_deg":{"median":4.4,"p90":11.5,"max":27.8},"H1_smoothing_benefit":{"interframe_motion_fitonly_mm":9.16,"interframe_motion_smoothed_mm":7.6,"reduction_pct":17},"A_ordering":{"fit_only":{"bone_cv_med_pct":0.142,"bone_cv_max_pct":0.367,"reproj_px":[7.38,4.01]},"fit_then_smooth_shipped":{"bone_cv_med_pct":1.036,"bone_cv_max_pct":6.157,"reproj_px":[21.65,17.18]},"smooth_then_fit":{"bone_cv_med_pct":0.137,"bone_cv_max_pct":0.293,"reproj_px":[21.89,17.23]}},"nan_resurrection":{"n_nan_nosmooth":2,"n_nan_smoothed":0,"n_resurrected":2,"which":[["0012","RIGHT_KNEE"],["0021","LEFT_ANKLE"]],"equals_prev_frame":[true,true],"dist_to_next_real_mm":[20.21,38.5],"pct_height":[17.2,32.7]},"H2":{"HEAD_fit_disp_median_mm":0.84,"HEAD_fit_disp_max_mm":3.39,"all_joint_fit_disp_median_mm":0.58,"HEAD_rank":5,"neck_head_raw":{"median_mm":21.86,"std_mm":2.08,"cv_pct":9.52,"min_mm":17.71,"max_mm":26.15,"range_pct":38.6},"HEAD_smoothing_disp_pct_of_segment":23.5,"neck_to_nose_rotation_deg":{"median":3.7,"max":9.9}},"H3_raw_bone_cv_pct":{"spine":4.91,"neck-head":9.52,"neck-Lsh":5.14,"neck-Rsh":5.01,"L_upper_arm":5.4,"R_upper_arm":3.93,"L_forearm":4.98,"R_forearm":5.47,"pelvis-Lhip":7.17,"pelvis-Rhip":7.17,"L_thigh":4.92,"R_thigh":5.8,"L_shin":3.11,"R_shin":5.67},"H3_targets_used_mm":{"spine":41.08,"neck-head":21.86,"neck-Lsh":15.97,"neck-Rsh":16.01,"L_upper_arm":22.41,"R_upper_arm":22.52,"L_forearm":21.35,"R_forearm":19.96,"pelvis-Lhip":8.32,"pelvis-Rhip":8.31,"L_thigh":37.17,"R_thigh":35.49,"L_shin":37.11,"R_shin":36.84},"H3_symmetry_raw":{"thigh":{"asym_pct":4.62,"diff_mm":1.68},"shin":{"asym_pct":0.72,"diff_mm":0.26},"upper_arm":{"asym_pct":0.48,"diff_mm":0.11},"forearm":{"asym_pct":6.72,"diff_mm":1.39}},"D_symmetric_targets":{"pose_change_median_mm":0.308,"pose_change_max_mm":0.59,"reproj_sym_px":[7.439,4.194],"reproj_asym_px":[7.381,4.008]},"H4":{"n_bones_falling_back":0,"fallback_thigh_m":0.43,"measured_thigh_m":0.03717,"ratio":11.6,"sim_missing_L_shin":{"target_used_m":0.44,"is_fallback":true,"other_joints_moved_median_mm":0.0,"max_mm":1.22,"L_knee_median_mm":0.31},"sim_fill_missing_true":{"ankle_placed_at_m":0.44,"subject_span_m":0.4722,"normal_span_m":0.1277}},"H5":{"NECK_2dmid_vs_3dmid_median_mm":0.254,"NECK_max_mm":0.396,"NECK_pct_height":0.216,"NECK_pct_of_span":0.8,"PELVIS_median_mm":0.078,"PELVIS_max_mm":0.108,"PELVIS_pct_height":0.066,"stored_2d_is_exact_pixel_midpoint_max_px":0.0,"shoulder_depth_diff_median_mm":7.91,"shoulder_depth_diff_max_mm":23.58,"camera_dist_m":0.559,"rel_depth_spread_pct":1.414},"H6":{"zero_px_drag_jump_mm":{"median":4.92,"mean":6.07,"p90":12.77,"max":25.65,"median_pct_height":4.18,"max_pct_height":21.78},"joints_moving_gt_1mm_per_drag":"15 of 15","frame_0014":{"dragged_LEFT_WRIST_moved_mm":21.02,"others_median_mm":8.93,"others_max_mm":25.65,"worst":"RIGHT_ANKLE"},"frame_0020":{"dragged_moved_mm":14.15,"others_median_mm":14.4,"others_max_mm":24.92},"neighbour_step_mm":{"before_prev":13.62,"after_prev":22.7,"ratio":1.67,"before_next":6.97,"after_next":6.93},"accuracy_gauge_px":{"gauge_reads_raw":[5.11,2.78],"displayed_fitted":[21.65,17.18],"ratio":4.24},"stale_derived":{"LEFT_HIP_60px_drag":{"PELVIS_2d_stale_px":30.0,"PELVIS_3d_err_mm":2.08,"pct_height":1.77},"LEFT_SHOULDER_60px_drag":{"NECK_2d_stale_px":30.0,"NECK_3d_err_mm":1.89,"pct_height":1.61},"mm_3d_per_px_one_view":0.178,"shoulder_span_px_left":224.2}},"H7":{"total_obs":390,"exactly_one_view":2,"pct":0.51,"which":[["0012","RIGHT_KNEE","left_only"],["0021","LEFT_ANKLE","left_only"]],"neither_view":0,"raw_3d_nan":2,"validate_would_drop_now":0,"repeat_drops":[0,0,0],"epi_threshold_px":71.5,"epi_median_px":4.91,"epi_max_px":29.38,"headroom_x":2.43},"H8":{"DLT":{"bone_cv_med_pct":5.272,"reproj_px":[5.107,2.783]},"midpoint":{"vs_DLT_median_mm":0.005,"vs_DLT_max_mm":0.1432,"bone_cv_med_pct":5.266,"reproj_px":[5.106,2.785]},"optimal_correctMatches":{"vs_DLT_median_mm":0.3725,"vs_DLT_max_mm":2.3229,"bone_cv_med_pct":5.452,"reproj_px":[2.36,4.318]}},"H9":{"PELVIS_fit_disp_median_mm":0.76,"PELVIS_max_mm":2.92,"all_joints_median_mm":0.59,"centroid_shift_median_mm":0.0,"per_frame_ms_lm":21.47,"per_frame_ms_trf_sparse":746.78,"slowdown_x":34.8,"n_variables":45,"n_bones":14,"max_abs_bone_target_residual_mm":0.01},"scores":{"left_median":0.88,"left_frac_below_0.35":0.0,"left_frac_below_0.60":0.01,"right_median":0.82,"right_frac_below_0.35":0.005,"right_frac_below_0.60":0.021}}

NEGATIVE: REFUTED — do not re-open these.

H5 (derived NECK/PELVIS as 2D midpoints are not the 3D midpoint under perspective): REAL BUT NEGLIGIBLE HERE. Triangulated NECK vs the 3D midpoint of the triangulated shoulders: median 0.254 mm, max 0.396 mm = 0.22 % of body height, 0.8 % of the 31.97 mm shoulder span. PELVIS: median 0.078 mm, max 0.108 mm = 0.066 % of height, 0.47 % of the 16.63 mm hip span. Verified the stored 2D really is the exact pixel midpoint (max deviation 0.0000 px in both views), so the metric is measuring the intended thing. Cause of the smallness: the two shoulders differ in depth by only 7.91 mm median / 23.58 mm max at a 0.559 m camera distance = 1.41 % relative depth spread, and the perspective error scales with that squared. Would matter for a full-size human at close range; it is ~1/20 of the smoothing error here. Artifacts: probe.json keys H5_midpoint, H5_depth_spread.

H8 (linear DLT is unweighted/unrefined; Sampson-optimal or midpoint would change the 3D): REFUTED. Three triangulators on the same 2D and calibration. Mid-point-of-common-perpendicular vs cv2.triangulatePoints: median 0.005 mm, max 0.143 mm difference; bone CV 5.266 % vs 5.272 %; reprojection 5.106/2.785 px vs 5.107/2.783 px. Hartley-Sturm optimal correction (cv2.correctMatches) + DLT: median 0.373 mm, max 2.323 mm difference; bone CV gets WORSE (5.452 % vs 5.272 %); reprojection redistributes between views (2.360/4.318 px vs 5.107/2.783 px) without improving the geometry. Replacing the triangulator buys nothing measurable; the sub-mm differences are two orders below the 25.65 mm smoothing error. Artifact: probe.json key H8_triangulators.

H9a (fit_bone_lengths lets 45 variables move freely with no root constraint, so the pelvis/root drifts): REFUTED. The fit (smooth=False) moves every joint by <1.3 mm: PELVIS median 0.76 mm, max 2.92 mm; all-joint median 0.59 mm; the pose CENTROID shift is 0.00 mm median — there is no root drift. Artifact: probe.json key H9_root.

H9b (bone_weight=5.0 vs data_weight=1.0 is too soft to enforce the lengths — the tests use 20.0): REFUTED. After fit_project(smooth=False), every bone's median length matches the target it was given to within 0.010 mm (worst: neck-head -0.010 mm; 13 of 14 within 0.009 mm), and the resulting bone CV is 0.142 % median / 0.367 % max. The weight is ample at this scale. Artifact: probe.json keys H9_bone_target_residual, H9_bone_weight.

H7 (validate_cross_view drops the lower-confidence view so joints vanish; the ray-constrained single-view placement promised in PROJECT_CONTEXT.md section 8 is missing): CONFIRMED AS A CODE GAP BUT NOT A DRIVER ON THIS TAKE. Grep confirms no single-view/ray-constrained placement exists anywhere (pipeline.py:100 simply NaNs the observation; triangulate.py has no such function). Counted on the client take: 390 (frame,joint) observations, exactly 2 present in one view only (0.51 %) — frame 0012 RIGHT_KNEE and frame 0021 LEFT_ANKLE, both left-only; 0 present in neither view; 2 NaN 3D joints, matching. Re-running validate_cross_view on the stored 2D drops 0 more, three times in a row, and the gate has 2.43x headroom (threshold 71.5 px, worst measured body epipolar 29.38 px, median 4.91 px). So the gate is inert on this take and implementing ray-constrained placement would fix 2 of 390 observations. NOTE: those same 2 joints are the ones the smoother then resurrects (Finding 2), which is where the 20-39 mm error actually comes from — fix the smoother, not the gate. Artifacts: probe.json keys H7_single_view, H7_validate_would_drop_now, EXTRA_validate_repeat_drops, EXTRA_epipolar.

H2 (the fixed NECK-HEAD bone displaces the nose a lot): PARTIALLY REFUTED — the segment is genuinely non-rigid (9.52 % CV, 38.6 % range, worst of 14 bones) but the fit only moves the nose 0.84 mm median / 3.39 mm max, ranking 5th of 15 joints against an all-joint median of 0.58 mm. Kept as a minor finding for its effect on head ORIENTATION (5.14 mm of smoothing displacement = 23.5 % of the 21.9 mm segment, 3.7 deg median neck->nose rotation), not as a body-accuracy driver.

H3 remedy (symmetrise the L/R fit targets): REFUTED AS A FIX. Forcing thigh/shin/upper-arm/forearm targets to their L/R mean changes the fitted pose by 0.308 mm median / 0.590 mm max and makes reprojection slightly worse (7.439/4.194 px vs 7.381/4.008 px). The asymmetry is a diagnostic of upstream error, not something the fit can correct. Artifact: probe2.json key D_symmetric_targets.

Detector confidence is NOT a gating problem here (checked while probing derive_joints, which has no confidence gate on its NECK/PELVIS midpoints): left view median score 0.88, 0.0 % below 0.35, 1 % below 0.60; right view median 0.82, 0.5 % below 0.35, 2.1 % below 0.60. No joint's median score is below 0.716 (HEAD, left). Artifact: probe.json key EXTRA_scores.

NOTES: METHOD. All numbers come from two scripts I wrote and ran against the real client take (workspace/pose3d_projects/Imported_Session, 26 frames, 0 corrections), importing the parent's baseline metrics.py so figures stay comparable:
- /tmp/claude-1000/-media-athena-hd3-Projects-pose3d-tool/0fcb8c9e-52c1-483d-9e0a-80896b078fa5/scratchpad/find-pipeline/pipeline_probe.py -> probe.json (H1-H9 + extras)
- /tmp/claude-1000/-media-athena-hd3-Projects-pose3d-tool/0fcb8c9e-52c1-483d-9e0a-80896b078fa5/scratchpad/find-pipeline/probe2.py -> probe2.json (smoothing order, drag simulation, stale derived joints, symmetric targets)
Nothing in the repository was modified; no packages installed; no GUI launched. Both scripts run in well under a minute.

RANKING RATIONALE. Findings 1-5 all trace to ONE decision — smooth_temporal on by default at alpha=0.6 over stop-motion frames — plus its interaction with the live-edit path. Flipping pipeline.py:153 to smooth=False fixes Findings 1, 3 (the jump disappears because the batch path stops smoothing) and 5 (ordering becomes moot), and takes reprojection from 21.65/17.18 px to 7.38/4.01 px and bone CV from 1.04 %/6.16 % to 0.14 %/0.37 %. It is a one-word change with the largest measured effect of anything in this dimension. Finding 2 (NaN resurrection) is a separate one-line deletion in the same function and should ship with it.

SCOPE / VALIDITY.
1. One take, 26 frames, 0 user corrections. Per-joint medians rest on 25-26 samples; every "max" column is a single frame. The live-edit findings (3, 7) are from a Qt-free reimplementation of ProjectModel._resolve_joint (probe2.py:resolve_joint) that reproduces model.py:150-161 line for line; I did not launch the GUI, so I confirmed the CODE PATH and simulated its arithmetic, not the on-screen pixels.
2. My retarget-facing claims (bone-direction rotation of 4.4-27.8 deg) assume the retargeting description given in my context (each bone rotates from REST to aim at its captured joint). I did not read character.py; that link is inference from the stated design, the rotation angles themselves are measured.
3. head2d/head3d are absent from this project, so every head-orientation consequence in Finding 8 is inferred from the neck->nose segment geometry, not observed end to end.
4. Reprojection error is a weak absolute metric for raw (a two-view DLT point reprojects near its own observations by construction). I use it only as a COMPARISON across pipeline stages: raw 5.11 -> fit-only 7.38 -> shipped 21.65 px (left). Do not quote the raw figure as an accuracy number.
5. This dimension does not explain everything. The raw triangulation's own bone CV is 5.3 % median before any of my findings apply, and the L/R asymmetry (6.7 % forearm on a symmetric mannequin) is upstream of every function I audited. My Finding 9 bounds this dimension's own systematic contribution at ~1.5 mm; the smoothing defect adds 6 mm on top. If the client still sees error after the smoother is fixed, the remainder is in calibration or detection, not here.
6. HEIGHT = 0.1178 m throughout (the z-extent of the de-tilted pose, the same reference the baseline uses), so my "% of height" figures are directly comparable to the baseline's.


## auditor (categories ['bone-fit', 'export', 'head', 'orientation', 'retarget', 'ui-feedback']) baseline_numbers: {"take":"workspace/pose3d_projects/Imported_Session","frames":26,"source":"fitted3d, de-tilted with de_tilt_matrix(sequence_up(fitted3d)), Character().fit_to_subject once","subject_height_m":0.11778,"fitted_scale":113.62,"char_joint_height_over_subject":1.052,"char_mesh_height_over_subject":1.201,"rig_mesh_h_over_joint_h":1.097,"scale_if_joint_height_matched":128.9,"scale_variants":{"shipped":113.62,"excl_NECK->HEAD":114.55,"excl_NECK->HEAD_and_PELVIS->HIP":114.34},"per_joint_retarget_pct_height_median":{"HEAD":12.58,"NECK":4.11,"LEFT_SHOULDER":1.02,"RIGHT_SHOULDER":1.05,"LEFT_ELBOW":1.90,"RIGHT_ELBOW":2.17,"LEFT_WRIST":1.32,"RIGHT_WRIST":0.74,"PELVIS":0.00,"LEFT_HIP":2.10,"RIGHT_HIP":3.50,"LEFT_KNEE":1.13,"RIGHT_KNEE":3.44,"LEFT_ANKLE":0.99,"RIGHT_ANKLE":0.75},"H1_roll_vs_captured_bendplane_deg_median_max":{"upper_arm.L":[53.6,72.8],"forearm.L":[20.4,49.0],"upper_arm.R":[13.6,41.2],"forearm.R":[14.4,30.2],"thigh.L":[21.2,37.3],"shin.L":[13.8,23.4],"thigh.R":[26.7,54.0],"shin.R":[25.4,48.3]},"H1_roll_after_prototype_deg":{"all_eight_bones_median_and_max":0.0},"H1_synthetic_roll_change_from_1deg_aim_wobble_deg":{"150":0.7,"160":1.1,"176":4.9,"178":9.7,"180":163.8,"182":9.4,"186":3.0},"bone_swing_from_rest_deg_median_max":{"upper_arm.L":[28.0,80.8],"forearm.L":[51.5,104.5],"thigh.R":[27.3,88.8],"shin.R":[31.1,110.5],"hips":[12.9,21.0],"chest":[15.7,24.5],"neck":[11.7,32.6]},"leaf_bones_no_aim":{"foot.L":{"axis_dir_spread_deg":59.6,"sole_tilt_deg_median":32.1,"sole_tilt_deg_max":74.0},"foot.R":{"axis_dir_spread_deg":95.9,"sole_tilt_deg_median":30.4,"sole_tilt_deg_max":108.0},"hand.L":{"axis_dir_spread_deg":125.9},"hand.R":{"axis_dir_spread_deg":27.6}},"grounding_ankle_above_lowest_vertex_pct_height":{"median":9.0,"min":4.0,"max":15.1,"peak_to_peak":11.2},"H2_rig_minus_subject_pct_of_bone":{"NECK->HEAD":-25.0,"LEFT_SHOULDER->LEFT_ELBOW":-8.8,"RIGHT_SHOULDER->RIGHT_ELBOW":-10.4,"LEFT_ELBOW->LEFT_WRIST":0.2,"RIGHT_ELBOW->RIGHT_WRIST":5.3,"PELVIS->NECK":11.1,"PELVIS->LEFT_HIP":20.4,"PELVIS->RIGHT_HIP":20.7,"LEFT_HIP->LEFT_KNEE":1.5,"RIGHT_HIP->RIGHT_KNEE":7.3,"LEFT_KNEE->LEFT_ANKLE":-5.6,"RIGHT_KNEE->RIGHT_ANKLE":-3.6},"H2_measured_over_predicted_from_length_mismatch":{"NECK":1.07,"LEFT_ELBOW":1.13,"RIGHT_ELBOW":1.07,"LEFT_WRIST":0.80,"RIGHT_WRIST":0.50,"LEFT_KNEE":0.74,"RIGHT_KNEE":1.20,"LEFT_ANKLE":0.46,"RIGHT_ANKLE":0.31},"per_bone_stretch_prototype":{"all_median_excl_HEAD_pct_height":{"before":1.41,"after":0.87},"LEFT_ELBOW":[1.90,0.50],"RIGHT_ELBOW":[2.17,0.17],"LEFT_WRIST":[1.32,0.60],"RIGHT_WRIST":[0.74,0.13],"RIGHT_KNEE":[3.44,1.89],"stretch_factors_range":[0.932,1.116]},"H3_hip_shoulder_yaw_deg":{"median":11.0,"p90":17.3,"max":21.7,"n":26},"H3_character_line_error_deg":{"hip_line_shipped":[11.8,21.2],"hip_line_rollproto":[4.5,14.7],"shoulder_line_shipped":[0.2,0.5]},"H4_up_pairwise_deg":{"sequence_up|cross_camera_x":9.7,"sequence_up|mean_minus_camera_y":22.2,"cross_camera_x|mean_minus_camera_y":14.2,"sequence_up|tag_row_cross_normal":18.1},"H4_tag_row":{"tags":[14,13,17],"row_len_m":0.298,"straightness_sv_ratio":0.093},"H4_frame_lean_vs_mean_deg":{"median":10.83,"max":21.10},"H5_head":{"project_has_head2d_head3d":false,"nose_off_torso_deg":{"median":39.6,"min":29.7,"max":55.1},"corrected_neck_aim_deg":{"median":0.3,"min":0.0,"max":15.9,"pct_clamped_at_zero":46.2},"rest_head_pitch_deg":5.73,"NOSE_PITCH_deg":45.0,"neck_to_HEAD_dir_err_deg":{"median":39.4,"min":31.9,"max":41.4},"HEAD_err_pct_height":{"shipped":12.58,"aim_straight_at_nose":1.72},"neck_to_HEAD_dir_err_deg_nofudge":{"median":7.5,"max":10.0}},"H6":{"exceptions_raised":0,"returns_None":52,"sparse_variants_tested":130,"None_condition":"PELVIS + LEFT_HIP + RIGHT_HIP all missing","real_take_failures":0},"H7":{"export_hips_translation_range_rig_units":[0.0,0.0,0.0],"captured_pelvis_travel_m":0.1362,"captured_pelvis_travel_pct_height":115.7,"view_dropout_shift_m":0.0,"frames_with_dropped_joint":0},"limb_bend_deg_180_is_straight":{"L_elbow":{"median":124.7,"min":117.4},"R_elbow":{"median":139.0,"min":133.7},"L_knee":{"median":135.3,"min":95.5,"pct_frames_over_160":11.5},"R_knee":{"median":146.8,"min":92.3,"pct_frames_over_160":3.8}},"rig":{"bones":19,"source":"MakeHuman base mesh (CC0) decimated + auto-rigged","verts":1752,"faces":3500,"rig_h_bone_heads":14.4228,"rig_joint_h":15.181,"rig_mesh_h":16.6469}}

NEGATIVE: REFUTED / NULL - do not re-open these.

H1 near-180 gimbal as an ACTIVE cause: refuted on this take. Max bone swing from rest over 26 frames is 110.5 deg (shin.R); medians 12-52 deg. The 163.8 deg roll flip from a 1 deg wobble only occurs within ~2 deg of exactly 180 (probe3.json). Reported as latent-only (finding 10). The roll problem that IS active is the ordinary, non-singular one (finding 1).

The bend-plane metric is NOT the way to see the roll defect: the baseline's bend-plane angle for fitted3d is 3.6 deg median / 14.9 deg max because both limb bones aim independently at their own captured joints, so the character's joint bend plane genuinely follows the capture. The defect is the twist ABOUT each bone axis, which is 13.6-53.6 deg and invisible to that metric. Do not conclude from the 3.6 deg figure that limb orientation is fine.

H7 view re-centring caused by dropouts: refuted on this take. fitted3d has no NaN in any of the 26 frames, so the mean-of-valid-joints centre equals the mean-of-all-joints centre and the dropout-induced shift is exactly 0.0 m. (The separate, confirmed H7 problem is that BOTH view and export discard the pelvis's 0.136 m of real travel, by different rules - finding 6.)

H6 exceptions: Character never raised on this take. 130 sparse-pose variants (26 frames x 5 dropout patterns) produced 0 exceptions and 52 (None,None,None) returns, all of them patterns that delete PELVIS + both hips; the take's real fitted3d posed in 26/26. So the blanket excepts are a diagnostics problem, not an active accuracy problem (finding 8).

H8 uniform scale is NOT mis-fitted. fit_to_subject returns scale 113.62; the posed character's canonical-joint height is 1.052x the subject's captured height and its mesh height 1.201x (the rig's own mesh-to-joint ratio is 1.097, so the mesh figure is expected, not a bug). Removing the two definitionally-mismatched bones from the least-squares (NECK->HEAD, which compares nose-to-neck against head-bone-mid-to-neck, and PELVIS->HIP) moves the scale by less than 1%: 113.62 -> 114.55 -> 114.34. The leg 2x weighting in _SCALE_WEIGHTS does apply correctly (Joint is an IntEnum so the int keys hash equal). No finding here.

Retarget error is not caused by the aim path being wrong. PELVIS lands at 0.00% error by construction, shoulders at ~1.0%, and the character's shoulder line matches the captured shoulder line to 0.2 deg median. The aim mechanism works; what it cannot express is length ratio (finding 3), roll (finding 1) and hip twist (finding 5).

Head error is NOT mostly definitional. The baseline caveat that HEAD's 12.6% is a definitional nose-vs-skull-point offset is only 4.6 percentage points of it (the NECK->HEAD length difference); the remaining ~8 points come from a 39.4 deg direction error produced by the _NOSE_PITCH fudge and its clamp (finding 2). Median approx max simply because the bias is constant, not because it is definitional.

Export/view pose agreement (as opposed to placement): the orientation maths does match. blender_export.py:61 uses R = de_tilt_matrix(up).T with poses3d @ R, which equals the view's poses @ de_tilt_matrix(up).T at view3d.py:106. Same fit_to_subject on the same de-tilted poses (blender_export.py:76). Only the root translation differs (finding 6).

NOTES: SCOPE / METHOD
- All numbers are on the real client take, source = stored fitted3d, oriented exactly as pose3d/ui/main_window.py:487-505 does (sequence_up -> de_tilt_matrix -> Character.fit_to_subject once over the whole take), so they are directly comparable to the supplied baseline (my all-joint median 1.51% and character scale 113.62 reproduce the baseline exactly).
- Repository untouched. Everything is under /tmp/claude-1000/-media-athena-hd3-Projects-pose3d-tool/0fcb8c9e-52c1-483d-9e0a-80896b078fa5/scratchpad/find-retarget/: retarget_probe.py + probe.json (H1 roll, H2 ratios, H3 twist, H4 up-vectors, H5 head, H7 recentre), probe2.py/.json (scale + mesh extent, leaf bones, H6 sweep, H7 export matrices), probe3.py/.json (head decomposition, limb straightness, H1 synthetic singularity, bone swing), probe4.py/.json (head alternative, ArUco tag-row gravity), probe5.py/.json (foot sole tilt, grounding), probe6.py/.json (H2 attribution, scale variants), roll_prototype.py/.json (H9b), scale_prototype.py/.json (H9a), render.py + contact_front.png + contact_side.png.
- matplotlib is NOT installed in .venv (only PIL), so render.py is a hand-written PIL painter's-algorithm renderer with Lambert shading. It reproduces View3D.set_pose's framing (de-tilt, centre on the mean of valid joints, ground on the lowest mesh vertex) and overlays the captured skeleton in orange and the character's read-back joints in cyan, 3 variants x 6 frames x 2 azimuths. Read the PNGs: the body tracks well, the FEET and HANDS are the visibly wrong parts (spike feet in shipped f0006/0011/0016/0021, splayed hands in f0026), and the no-nose-fudge row visibly pitches the head down - which is why finding 2's fix is 'unclamp + fix the reported point', not 'aim at the nose'.
- Both prototypes are pure subclasses of Character; neither touches the repo. RollCharacter copies _skin_matrices verbatim with one added roll step, so its 'before' column reproduces the shipped numbers exactly - the deltas are attributable.

RANKING RATIONALE
Findings 1, 2 and 4 are the ones a client sees without measuring anything (limb twist, a frozen head, feet that spike and a figure that bobs). Finding 3 is the largest thing that shows up in the numbers but is only ~1.4% of height - it is the accuracy floor, not the complaint. Findings 5-7 are structural (twist unrepresentable, dead export root, dead API). Findings 8-10 are latent or diagnostic.

INTERACTION WITH OTHER AUDITORS
- The retarget error is measured against the same source it is posed from, so it cannot see error already in fitted3d. The baseline shows the EMA smoother contributes 98% of the 6.22 mm between measurement and shipped pose and makes reprojection 4x worse; that is upstream of me. My findings are additive to it, not alternatives.
- Finding 3's fix (a) and the smoothing/bone-fit dimension interact: if fitted3d changes, refit fit_to_subject and re-measure the stretch factors (they are take-scoped, computed once from measure_bone_lengths).
- Finding 2 depends on the detector dimension: the real fix is head2d/head3d existing, which needs a re-detect path for already-imported projects. Worth flagging to whoever owns detection.

CONFIDENCE CAVEATS
- One take, 26 frames, 0 user corrections. Per-joint medians rest on 26 samples; every 'max' is one frame.
- The tag-row gravity candidate (finding 9) uses ONE image and 3 of the 4 tags, with the single-tag IPPE ambiguity unresolved, and the tags are taped to a CURVED sweep so the row is not guaranteed horizontal. Treat the 18.1 deg as one more disagreeing candidate, not as ground truth.
- I could not run Blender, so finding 6's export claim is measured on the bone matrices the exporter hands to Blender (hips translation range exactly [0,0,0] over 26 frames) plus a read of blender_job.py:468, not on a produced BVH.


## auditor (categories ['calibration-extrinsics', 'calibration-intrinsics', 'cross-view-validation', 'orientation', 'robustness']) baseline_numbers: {"provenance":{"shipped_rig_is":"tag 14, frame 0001, IPPE solution 0, both cameras","match_rot_deg":0.0,"match_trans_mm":0.0,"ippe_err_ratio_frame0001_tag14":{"left":11.92,"right":9.27},"branch_separation_deg":{"left":63.69,"right":67.47}},"image_sizes":{"left":[3072,4080],"right":[1536,2048],"left_exif_FL35mm":24,"right_exif":"none"},"tag_visibility":{"left":{"13":26,"14":26,"17":26,"15":6},"right":{"14":11,"15":2,"none":15},"n_frames":26,"total_tag_solves":97},"pnp_per_tag_assumed_K":{"left:14":{"n":26,"dist_m":0.638,"dist_std_mm":0.99,"normal_spread_deg":0.81,"frac_ambig_ratio_lt2":0.0,"min_ratio":5.87},"left:13":{"n":26,"dist_m":0.718,"dist_std_mm":2.51,"normal_spread_deg":46.05,"frac_ambig_ratio_lt2":0.962,"min_ratio":1.024},"left:17":{"n":26,"dist_m":0.788,"normal_spread_deg":0.71,"frac_ambig_ratio_lt2":0.0},"left:15":{"n":6,"dist_m":0.595,"normal_spread_deg":0.41},"right:14":{"n":11,"dist_m":0.631,"dist_std_mm":0.77,"normal_spread_deg":0.67,"frac_ambig_ratio_lt2":0.0,"min_ratio":9.27},"right:15":{"n":2,"dist_m":0.693}},"all_solves":{"n":97,"ratio_median":8.74,"ratio_min":1.024,"frac_ratio_lt_2":0.258,"best_reproj_err_median_px":0.512},"single_tag_relpose_spread_over_11_frames":{"rel_rot_spread_deg":1.129,"baseline_m_range":[0.5496,0.5590],"baseline_spread_mm":9.49,"baseline_std_mm":3.19,"frame0001_rot_to_mean_deg":0.696},"tag_layout_rel_tag14":{"15":{"centre_m":[0.0178,-0.1069,-0.0004],"normal_deg":1.81,"inplane_Y_deg":90.77,"full_rot_deg":90.8},"13":{"centre_m":[-0.0317,0.1561,-0.0004],"normal_deg":74.45,"inplane_Y_deg":113.2,"full_rot_deg":114.7},"17":{"centre_m":[-0.0144,0.2982,0.0131],"normal_deg":15.15,"inplane_Y_deg":165.92,"full_rot_deg":179.4},"max_normal_spread_deg":53.39},"BA":{"unknowns":30,"n_residuals":776,"loss":"huber f_scale=2","rms_init_px":6.814,"rms_px":3.156,"median_abs_resid_px":1.598,"nfev":21,"rot_vs_shipped_deg":{"left":0.561,"right":1.154},"rel_rot_vs_shipped_deg":0.696,"cam_centre_shift_mm":{"left":5.92,"right":12.55},"baseline_m":{"shipped":0.5590,"ba":0.5588},"drop_right_tag15_changes_rel_rot_deg":0.849,"BA_exif":{"rms_px":3.261,"rel_rot_vs_shipped_deg":13.306,"baseline_m":0.3478}},"square_holdout_tag15_true_50.00mm":{"SHIPPED_assumed":50.29,"BA_full_assumed":50.30,"BA_holdout_assumed":50.49,"BA_bestfocal":50.14,"BA_full_exif":49.80,"BA_holdout_exif":49.36,"SHIPPED_exif":64.56,"tag14_assumed":{"SHIPPED":49.91,"BA":49.98,"std_mm":0.05}},"rig_comparison_retriangulated":{"shipped":{"bone_cv_med_pct":5.272,"bone_cv_max_pct":9.524,"bone_cv_med_fitted_pct":0.142,"spine_m":0.04108,"height_m":0.11796,"asym_mean_pct":3.134,"asym":{"thigh":4.619,"shin":0.716,"upper_arm":0.483,"forearm":6.718},"epi_median_px":4.908,"epi_p90_px":12.484,"epi_p99_px":23.97,"epi_max_px":29.38,"reproj_L_px":5.107,"reproj_R_px":2.783,"retarget_median_pct":1.776,"retarget_p90_pct":5.356,"bend_med_deg":3.146,"gate_dropped":0},"ba_assumed":{"bone_cv_med_pct":5.362,"bone_cv_max_pct":9.438,"bone_cv_med_fitted_pct":0.139,"spine_m":0.0414,"height_m":0.11901,"asym_mean_pct":2.901,"epi_median_px":5.847,"epi_p90_px":14.705,"epi_p99_px":25.66,"reproj_L_px":6.056,"reproj_R_px":3.311,"retarget_median_pct":1.762,"retarget_p90_pct":5.363,"bend_med_deg":3.182,"gate_dropped":0,"mm_vs_shipped_median":2.384,"mm_vs_shipped_max":4.398,"shape_mm_vs_shipped_median":0.158,"shape_mm_vs_shipped_max":0.506},"ba_exif":{"bone_cv_med_pct":7.413,"height_m":0.12717,"asym_mean_pct":5.628,"epi_median_px":22.731,"epi_p90_px":43.75,"reproj_L_px":21.767,"reproj_R_px":13.431,"retarget_median_pct":2.478,"gate_dropped":1,"shape_mm_vs_shipped_median":2.133},"shipped_ext_exif_K":{"bone_cv_med_pct":5.129,"height_m":0.15596,"epi_median_px":6.261,"reproj_L_px":6.555,"reproj_R_px":3.577,"shape_mm_vs_shipped_median":0.939,"note":"invalid combination, extrinsics were solved at the assumed K"}},"focal_sweep_BA_scored_by_body_epipolar_median_px":{"grid_scale_of_max_wh":[0.6,0.7,0.8,0.9,1.0,1.1,1.2],"diagonal_sl_eq_sr":[31.12,22.34,14.41,7.08,5.85,7.01,8.95],"minimum":{"epi_median_px":5.369,"scale_left":0.9,"scale_right":0.8,"f_left":3672.0,"f_right":1638.4},"at_1.0_1.0":5.85,"at_exif_0.694_0.694":10.9,"shipped_single_tag_1.0_1.0":4.908,"ba_rms_px_range_over_grid":[3.13,3.40]},"extrinsic_sensitivity_rot_right_mean_of_5_axes":{"deg":[0.5,1,2,5,10,20,45,63.7],"epi_median_px":[5.11,5.61,7.79,16.10,33.05,79.04,254.17,413.36],"gate_dropped_pct":[0.0,0.0,0.0,0.0,11.82,46.54,78.2,90.2],"bone_cv_med_pct":[5.25,5.25,5.27,5.34,5.96,8.09,16.77,33.46],"shape_pct_of_height":[0.06,0.13,0.26,0.65,1.35,2.86,6.97,11.08],"slope_pct_height_per_deg":0.13},"wrong_ippe_branch":{"left":{"rot_deg":63.69,"err_ratio":11.92,"epi_median_px":284.59,"gate_dropped_pct":94.6,"bone_cv_med_pct":12.27,"height_m":0.1074},"right":{"rot_deg":67.47,"err_ratio":9.27,"epi_median_px":214.0,"gate_dropped_pct":84.3,"bone_cv_med_pct":19.78}},"gravity_H4":{"world_up_tilt_deg_quality_py":89.34,"tag14_Y_to_cam_x_cross_deg":98.54,"tag14_Y_to_cam_up_mean_deg":98.82,"tag14_X_to_cam_x_cross_deg":162.79,"tag14_Y_to_tagrow_deg":5.54,"tagrow_to_cam_x_cross_deg":93.01,"two_cam_proxies_disagree_deg":14.17,"cam_up_left_vs_right_deg":14.30,"up_from_tagrow_cross_normal":[-0.9806,-0.0956,0.1713],"up_row_to_cam_x_cross_deg":5.90,"up_row_to_cam_up_mean_deg":9.76,"sequence_up_world":[-0.9362,-0.0254,0.3505],"sequence_up_to_cam_x_cross_deg":8.97,"sequence_up_to_cam_up_mean_deg":21.09,"sequence_up_to_up_row_deg":11.33},"H5_essential_from_body":{"n_correspondences":388,"RANSAC":{"inliers":115,"rot_err_deg":5.72,"trans_dir_err_deg":5.62},"LMEDS":{"inliers":368,"rot_err_deg":13.81,"trans_dir_err_deg":8.13},"design_sv_s8_over_s9":1.28},"H6_thresholds":{"in_use_px":71.5,"frac_of_diag":0.014,"image_diag_px":5107.2,"shipped":{"median":4.908,"p90":12.484,"p99":23.968,"max":29.384,"n":388,"thr_5x_median":24.5,"thr_3x_p99":71.9},"ba_assumed":{"median":5.847,"p99":25.657,"thr_5x_median":29.2},"bestfocal":{"median":5.369,"p99":22.114,"thr_5x_median":26.8}},"scale_anchor":{"mannequin_nose_to_Lankle_in_tag_widths":2.395,"same_ratio_in_2D_left_px":"790.6/330.1 = 2.395","implied_m_if_tag_is_50mm":0.1198,"marker_length_default_m":0.05,"note":"absolute scale is set solely by the marker_length spinbox and is not stored anywhere"},"camera_static_check_mm":{"left_tag14_C_std":[2.77,1.96,1.13],"left_tag17_C_std":[3.44,2.66,1.98],"right_tag14_C_std":[3.59,5.35,4.98]}}

NEGATIVE: REFUTED, with numbers, so the synthesis does not re-open them.

H1 (the shipped rig sits on the planar-pose ambiguity) -- REFUTED. The shipped rig IS one tag in one frame: an exhaustive match over all 97 (frame,tag,camera) x 2 IPPE solutions identifies it as tag 14, frame 0001, solution 0, for BOTH cameras, to 0.0000 deg and 0.0000 mm. But that solve is nowhere near the ambiguity: the second solution's reprojection error is 11.92x the first in the left view and 9.27x in the right (the two branches differ by 63.7 / 67.5 deg). And it agrees with the other frames: over the 11 frames where the right camera also sees tag 14, frame 0001's tag-14 pose is within 1.32 deg (left) / 1.33 deg (right) of every other frame's, and the camera centre moves at most 12.9 / 14.0 mm. The frame lottery costs 1.13 deg of relative rotation = 0.15% of body height in shape. (Tag 13 IS ambiguous -- 96% of its solves, 46 deg normal flips -- but tag 13 is never common to both cameras, so it never reached the world-frame choice. That risk is reported as a separate latent finding.)

H2 (pooling all frames x tags x cameras into a bundle materially improves the rig) -- REFUTED on this take. I built it (calib_ba.py: 30 unknowns, 776 corner residuals, Huber, scipy least_squares; rms 6.81 -> 3.16 px in 21 evaluations), wrote it out as an app-format calibration folder, re-triangulated the stored 2D with it and ran the baseline metrics. BA vs shipped: relative rotation 0.696 deg, baseline 0.5588 vs 0.5590 m; 3D shape change after similarity Procrustes 0.158 mm median / 0.51 mm max = 0.13% of body height; raw bone CV median 5.27 -> 5.36%, max 9.52 -> 9.44%; L/R asymmetry mean 3.13 -> 2.90% (thigh 4.62->4.47, shin 0.72->0.13, upper arm 0.48->0.27, forearm 6.72->6.74); retarget median 1.78 -> 1.76% of height, p90 5.36 -> 5.36; bend plane 3.15 -> 3.18 deg median; gate drops 0 -> 0. Body epipolar gets slightly WORSE (4.91 -> 5.85 px median), and so does reprojection (5.11/2.78 -> 6.06/3.31 px). Adding the right camera's tag-15 observations (its only second tag, 2 frames) shifts the relative rotation by 0.849 deg -- the same order as the whole BA-vs-shipped difference, i.e. the rig is uncertain at ~1 deg no matter how it is solved. Independent holdout: a BA that never sees right-camera tag 15 reconstructs tag 15's 50.00 mm square cross-view at 50.49 mm; the SHIPPED one-tag rig scores 50.29 mm on the same holdout (planarity 0.13 mm, diagonal/side 1.4142 for both). Conclusion: the shipped single-tag rig is already within ~0.6% on an unseen 50 mm object; extrinsics are at the noise floor and cannot explain the client complaint.

H2 at the EXIF focal -- the BA remains self-consistent on TAGS (rms 3.26 px, tag-15 holdout 49.36 mm) but the body keypoints reject it: epipolar median 22.73 px, p90 43.75, bone CV 7.41%, asymmetry mean 5.63%, retarget 2.48% of height, 1 observation dropped by the gate. Simply swapping the EXIF K onto the SHIPPED extrinsics is worse still (reprojection 145.5/100.2 px, height 0.156 m) because those extrinsics are PnP-derived at the assumed K -- do not evaluate that combination.

H3 (the multi-tag path's same-rotation assumption is false here) -- CONFIRMED and reported as a finding. Measured full relative rotations to tag 14: tag 15 = 90.8 deg, tag 13 = 114.7 deg, tag 17 = 179.4 deg; normals 1.8 / 74.5 / 15.2 deg apart; max spread of the four normals about their mean 53.4 deg. The path also has zero callers in the app (grep: `tag_world_positions` appears only inside extrinsics.py; estimate_extrinsics is called only by tests/test_extrinsics.py:59 in single-tag mode).

H4 (the world up is arbitrary) -- CONFIRMED and reported as the top finding. quality.world_up_tilt = 89.34 deg on the shipped rig; check_rig fires all three of its warnings on this take. Three independent gravity proxies agree to 5.9-14.2 deg, which is far better than the 92 deg the sequence_up docstring cites (that number compares tags' own axes, not gravity).

H5 (recover the rig from body keypoints when a camera sees no tag) -- REFUTED as an accuracy path. findEssentialMat on all 388 body correspondences: RANSAC 115/388 inliers, 5.72 deg rotation error, 5.62 deg translation-direction error; LMEDS 368/388 inliers, 13.81 deg / 8.13 deg. 8-point design matrix s8/s9 = 1.28 (singular values ... 0.0430, 0.0336): the essential matrix is barely identifiable from a 0.128 m subject with static cameras. Scale is unrecoverable without a tag. Keep the current fail-to-2D behaviour.

H6 (the 71.5 px gate is sized for a bad calibration) -- CONFIRMED quantitatively. Observed body-keypoint distribution under the shipped rig: median 4.91, p90 12.48, p99 23.97, max 29.38 px over 388 pairs; the threshold is 14.6x the median and 3.0x p99, and 0/388 pairs exceed it. Perturbation shows the gate first fires between 5 and 10 deg of extrinsic error. Every rig I built (BA 25.66 px p99, best-focal BA 22.11 px p99) justifies the same 25-30 px data-driven threshold.

NOT a defect: camera motion. Per-tag camera-centre standard deviations across all 26 frames are 2.8/2.0/1.1 mm (left, tag 14), 3.4/2.7/2.0 mm (left, tag 17), 3.6/5.4/5.0 mm (right, tag 14); tag-to-camera distance std 0.8-2.5 mm. The cameras did not move.

METHOD NOTE for the synthesis: metrics.py --calib does NOT change the 3D. Source 'raw' returns take['pose3d'] verbatim (metrics.py:184) and 'refit'/'refit-nosmooth' fit the stored pose3d, so --calib only re-scores epipolar and reprojection. Running the CLI across four calibration folders gave IDENTICAL bone CV, symmetry, height, retarget and bend-plane in every column -- that is an artefact, not a result. I re-triangulated explicitly with M.triangulate(kp2d, rig) in eval_rigs.py; those are the numbers quoted above.

NOTES: SCOPE / VALIDITY

1. Everything below was run against workspace/pose3d_projects/Imported_Session with the stored kp2d (no RTMPose re-run). The repository was not modified; all outputs are under /tmp/claude-1000/-media-athena-hd3-Projects-pose3d-tool/0fcb8c9e-52c1-483d-9e0a-80896b078fa5/scratchpad/find-extrinsics/.

2. NEW FACT the earlier context had wrong, and it matters for anyone re-running this: the two cameras are NOT both 3072x4080. Left is 3072x4080 (Pixel 10 Pro, EXIF FL35=24 mm); RIGHT is 1536x2048 with no EXIF at all, and right_intrinsics.json is K=[[2048,0,768],[0,2048,1024],[0,0,1]]. My first pass applied the left K to the right camera and the shipped right extrinsics matched nothing (4.35 deg / 670 mm off); with the correct per-camera K the match is exact (0.0 deg / 0.0 mm). Any experiment that assumes a common K on this take is wrong. 'f=2833 for both' is not a meaningful EXIF hypothesis -- the right camera has no EXIF; I tested it as a common SCALE of 0.694 x max(w,h).

3. metrics.py --calib cannot answer 'what does a different calibration do to the 3D'. Source 'raw' returns take['pose3d'] verbatim (metrics.py:184); 'refit'/'refit-nosmooth' fit that same stored pose3d. Only the epipolar and reprojection blocks depend on the rig. My first run across four calibration folders produced identical bone CV / symmetry / height / retarget / bend-plane in every column; that is the artefact, not a result. All 3D-dependent numbers I report come from eval_rigs.py, which calls M.triangulate(kp2d, rig) per rig. If the synthesis wants the CLI outputs anyway they are in m_<rig>_<source>.json, but treat their bone/retarget columns as duplicates of the shipped column.

4. The BA prototype (calib_ba.py) treats the scene as fully static: 2 camera poses + 3 tag poses relative to tag 14, 30 unknowns, every detected tag corner as a residual, Huber f_scale=2. That is justified here (per-tag camera-centre std 1-5 mm across 26 frames) but the model has no per-frame camera pose, so it would need extending for a session where the cameras move. The ambiguity for tag 13 is resolved by initialising from the least-ambiguous solve and letting the bundle settle it; because a tag seen by only ONE camera adds 6 unknowns and constrains only that camera, tags 13/17 contribute almost nothing to the RELATIVE pose -- the only inter-camera links are tag 14 (26 left / 11 right frames) and tag 15 (6 left / 2 right). That is why pooling buys so little.

5. The tag-15 cross-view holdout is the only genuinely independent geometric test I found on this take (the right camera's tag-15 observations are used by no rig being compared, and the true side length is known). It exists in exactly 2 frames (0024, 0026) and only frame 0026 has tag 15 in both views with usable corners in my run, so the holdout rests on ONE square. Its verdict (shipped 50.29 mm vs true 50.00) should be read as 'the rig is right to about half a percent', not as a precise figure.

6. The gravity proxies are proxies. The camera-x-cross and camera-up-mean proxies disagree by 14.2 deg, which equals the two cameras' relative roll, so neither is better than about +/-14 deg on its own. The tag-row proxy is independent of camera roll and lands 5.9 deg from the x-cross proxy, which is the strongest evidence I have that both are roughly right -- but I have no actual gravity measurement, so 'sequence_up is 9-11 deg off vertical' carries at least +/-6 deg of proxy uncertainty. The direction of the finding (the world up is ~90 deg off and a usable gravity estimate exists) is solid; the exact 9 deg is not.

7. The perturbation study rotates only the RIGHT camera and averages over 5 random axes; the 0.13% of body height per degree slope is therefore an average over axis directions, not a worst case, and it is measured on this subject at this depth with this 0.559 m baseline. It should not be extrapolated to a different rig geometry.

8. The focal sweep is coarse (0.1 steps, 49 BA runs) and the criterion (body-keypoint epipolar error) is exactly the criterion commit f4c92f9 used, so it is not a fully independent confirmation of that decision -- it is the same criterion with the extrinsics re-optimised at each focal instead of held at the assumed-K solve, which removes the circularity the earlier comparison had. The absolute focal is weakly determined (the valley is a curved ridge); the ratio is better determined. Absolute metric scale comes from marker_length and is untouched by the focal, so the '12 cm vs 20 cm mannequin' question is not a focal question -- I measured that the mannequin is 2.395 tag widths tall in the raw left image, identical to the 3D ratio, so the reconstruction is exactly as large as the tag size says it should be.

9. Head orientation is invisible in everything I measured: this project has no head2d/head3d, so retarget_error posed the character with head_pts=None throughout.

10. One take, 26 frames, 0 user corrections. The manual-correction re-solve path (pose3d/ui/model.py) and any session where the cameras move are untested by these numbers.

11. BOTTOM LINE for the synthesis: extrinsic calibration is not the cause of the client's complaint on this take. The rig is uncertain at <=1.1 deg and the reconstruction's shape changes by 0.13% of body height per degree, so the entire extrinsics dimension is bounded at ~0.15% of body height -- against the baseline audit's finding that the EMA smoother alone displaces joints by 5.3% of height on average and 21.7% at worst. The one extrinsics-dimension item that could plausibly be client-visible is orientation: the world up is 89 deg off gravity, the app has no gravity reference at all, and orient.sequence_up both leaves a ~9-11 deg residual tilt and deliberately erases any lean held across the take.


## auditor (categories ['detection', 'head', 'testing', 'ui-feedback']) baseline_numbers: {"images":{"left":"3072x4080","right":"1536x2048","f_over_W_left":1.328,"f_over_W_right":1.333,"figure_h_px_left_median":775.6,"figure_h_px_left_range":[646.1,1031.3],"figure_h_px_right_median":406.7,"figure_h_px_right_range":[320.7,561.2]},"reddot_detector":{"mask":"H<=12 or H>=168, S>=110, V>=60, R-G>=45, R-B>=25, extent>=0.55, aspect<=2.0, area in [1.2e-4,6e-3]*figh^2","median_blobs_per_frame":{"left":16.0,"right":7.0},"n_associations":{"left":285,"right":153,"both_views":129}},"rtmpose_vs_reddot_2D":{"left_median_pctH":2.53,"left_p90_pctH":4.65,"right_median_pctH":3.53,"right_p90_pctH":6.36,"left_median_px_range_per_joint":[12.4,37.8],"right_median_px_range_per_joint":[7.2,25.6],"systematic_median_pctH":2.42,"random_MAD_median_pctH":2.66},"bone_frame_offset_pct_of_bone_len":{"left":{"ELBOW":[-10.1,-7.7],"WRIST":[-10.5,-10.9],"ANKLE":[-11.4,-9.0],"HIP":[22.6,5.1],"KNEE":[3.2,3.7],"SHOULDER":[8.5,-0.5],"NECK":-1.6},"right":{"ELBOW":[-4.3,-28.1],"WRIST":[-14.3,-6.9],"ANKLE":[-13.6,-5.1],"HIP":[12.7],"KNEE":[5.5,-0.3],"SHOULDER":[-19.4,11.0],"NECK":-0.5},"distal_signs_negative":"12/12","median_abs_along_pct":8.5,"median_abs_across_pct":5.7},"impact3d_rtmpose_vs_reddot":{"n_joint_frames":129,"median_mm":4.01,"p90_mm":6.11,"max_mm":12.95,"pct_body_height_median":3.4,"per_joint_median_mm":{"NECK":4.35,"L_SHOULDER":4.42,"R_SHOULDER":3.31,"L_ELBOW":5.01,"R_ELBOW":4.87,"L_WRIST":3.01,"R_WRIST":2.61,"PELVIS":3.96,"L_HIP":5.73,"R_HIP":4.13,"L_KNEE":4.84,"R_KNEE":4.61,"L_ANKLE":4.92,"R_ANKLE":6.11}},"hybrid_retriangulation":{"rtmpose":{"boneCV_med":5.27,"boneCV_max":9.52,"epi_px":4.91,"reprojL":5.11,"reprojR":2.78,"sym":{"thigh":4.62,"shin":0.72,"upper_arm":0.48,"forearm":6.72}},"snap_any":{"boneCV_med":10.79,"boneCV_max":29.44,"epi_px":6.07},"snap_both":{"boneCV_med":8.85,"boneCV_max":14.19,"epi_px":4.74},"gate_both_129":{"boneCV_med":1.87,"epi_px":4.29,"reprojL":4.37,"reprojR":2.43},"snap_both_only_129":{"boneCV_med":5.65,"epi_px":3.59,"reprojL":3.67,"reprojR":2.06}},"bone_frame_correction":{"baseline_boneCV":5.27,"alpha0.5_full":5.63,"alpha0.5_along":5.40,"alpha1.0_full":6.37,"alpha1.0_along":5.22,"verdict":"no consistent improvement"},"effective_resolution":{"det_input_size":[640,640],"pose_input_size":[192,256],"figure_rows_of_256":[119.0,128.5,140.0,148.5,158.0,159.0],"orig_px_per_model_px":{"left":[5.56,6.53,6.62],"right":[2.54,3.53,3.60]},"one_model_px_pctH":[0.63,0.84]},"detector_probe":{"stored_vs_fresh_median_px":0.0,"stored_vs_fresh_max_px":2.78,"fullframe_vs_crop_median_px":[1.87,2.09,2.75,3.10,4.19,5.47],"fullframe_vs_crop_median_pctH":[0.27,0.90],"fullframe_vs_crop_max_px":25.52,"bbox_jitter_sigma":0.03,"bbox_jitter_spread_median_px":[1.26,2.79],"bbox_jitter_spread_median_pctH":[0.22,0.49],"bbox_jitter_spread_max_px":15.16,"n_yolox_boxes":1},"halpe26_vs_coco17":{"direct_joints_median_move_pctH":[0.31,0.87],"HEAD_move_pctH":[10.01,10.98,11.43,12.04,12.21,15.85],"NECK_move_pctH":[3.42,3.45,4.39,4.57,5.73,6.29],"PELVIS_move_pctH":[0.62,1.76,2.05,2.10,3.16,3.67],"halpe_scores_head_neck_pelvis":[0.79,1.05],"weights_present_locally":true},"scores_and_gates":{"left":{"min":0.555,"p05":0.709,"median":0.880,"frac_below_0.2":0.0,"frac_below_0.60":0.0103,"n_nan":0,"min_edge_margin_px":621.6},"right":{"min":0.0,"p05":0.679,"median":0.820,"frac_below_0.2":0.0051,"frac_below_0.60":0.0205,"n_nan":2,"nan":["0012/RIGHT_KNEE","0021/LEFT_ANKLE"],"min_edge_margin_px":67.9}},"velocity":{"left_median_pctH":5.20,"left_p90":20.84,"left_max":72.06,"left_outliers_4x":38,"right_median_pctH":7.12,"right_p90":26.57,"right_max":83.07,"right_outliers_4x":35,"note":"outliers frame-synchronous across both cameras = real motion"},"ui_feedback":{"formula":"100*exp(-err/6)","ACC_HIGH":85.0,"ACC_MED":70.0,"green_needs_err_px":0.98,"amber_needs_err_px":2.14,"err_px_median":3.96,"ui_pct_median":51.6,"bands":{"green":48,"amber":58,"red":284},"band_pct":{"green":12.3,"amber":14.9,"red":72.8},"worst_joints_ui_pct":{"LEFT_ANKLE":24.0,"RIGHT_HIP":29.5,"LEFT_WRIST":36.0,"RIGHT_ANKLE":36.3},"corr_confidence_vs_offset":-0.288,"corr_ui_accuracy_vs_offset":-0.063,"offset_pctH_by_conf_tercile":[3.52,2.48,2.55],"conf_terciles":[0.834,0.902]},"midpoint_bias":{"NECK_2Dmid_vs_3Dmid_mm":0.254,"NECK_max_mm":0.396,"NECK_pctH":0.22,"PELVIS_mm":0.078,"PELVIS_pctH":0.07,"epi_derived_vs_parents_px":{"NECK":[1.51,3.86],"PELVIS":[4.78,6.70]},"torso_cv_pct":{"shipped":4.85,"from3Dmid":4.82}},"project_keys":["frame_id","images","kp2d","scores","pose3d","fitted3d","corrected"],"head2d_present":false}

NEGATIVE: REFUTED — do not re-open these.

H2 (effective resolution / downsampling is the bottleneck): REFUTED, quantitatively. Read from rtmlib's own MODE tables via pose3d.detect.models: balanced = YOLOX-m det_input_size (640,640) + RTMPose-m pose_input_size (192,256). I called rtmlib's own `RTMPose.preprocess` on the real YOLOX boxes for 3 frames x 2 cameras: the nose-to-ankle span occupies 119.0/128.5/140.0/148.5/158.0/159.0 of the 256 model rows, i.e. 1 model-input pixel = 2.54-6.62 original px = 0.63-0.84% of figure height in BOTH cameras (identical, because RTMPose normalises the crop — the right camera's half resolution costs nothing at the pose stage). SimCC's 2x split ratio puts the quantisation floor near 0.4% of figure height ~= 0.4-0.5 mm of 3D. The measured RTMPose-to-marker discrepancy is 4.0 mm — 8-10x that floor. So resolution is not the limit; the model's notion of where the joint is, is. Corollary experiments: (a) tight-crop-with-20%-margin re-detection moves keypoints by only 1.87-5.47 px median (0.27-0.90% of figure height; max 25.5 px on one joint), so cropping before detection buys ~nothing; (b) perturbing the person box by sigma=3% of its size, 12 draws, moves keypoints by 1.26-2.79 px median (0.22-0.49% of figure height, max 15.2 px) — RTMPose is stable to box noise, so the 2.66% "random" component of the marker offset is NOT detector jitter. Untested and still open: mode="performance" would give pose_input_size (288,384), 1.5x the effective resolution, but its checkpoints are not in the local cache and I was not allowed to download.

H3 (hybrid: RTMPose for association + red-blob centroid for position improves the 3D): REFUTED as a drop-in. Re-triangulating under the shipped calibration (n=26 frames): raw bone-length CV median 5.27% / max 9.52% for RTMPose; snapping both views to the associated dot gives 8.85% / 14.19% (WORSE); snapping per-view wherever a dot exists gives 10.79% / 29.44% (much worse — it mixes joint definitions across views). Cause: the dots are on the limb SURFACE, so as the limb rotates the inter-dot distance genuinely changes; a marker detector needs a per-joint radial offset along the limb axis, not a bare snap. What DID improve, on the same joint subset: epipolar error 4.29 px (RTMPose) -> 3.59 px (dots), a 16% reduction, and left/right reprojection 4.37/2.43 -> 3.67/2.06 px; so the dots ARE the more cross-view-consistent 2D signal, which is why finding 1 proposes them with an axial correction rather than raw.

Inverting the measured systematic bias (apply each (camera, joint)'s median along/across-bone offset as a pose-adaptive correction): REFUTED at this measurement quality. alpha=1.0 full bone-frame correction: bone CV 5.27% -> 6.37%, max 9.52% -> 14.55%; along-only alpha=1.0: 5.22% (flat). Symmetry moves inconsistently — forearm asymmetry improves 6.72% -> 0.6-2.8% but upper arm degrades 0.48% -> 8.9-21.6%. The per-joint offset table has only 3-26 samples per (camera, joint) and the greedy dot association mis-assigns sometimes, so it is not accurate enough to invert. The bias measurement itself stands (12/12 distal joints negative across both cameras); only the naive inversion fails.

Perspective bias of the derived NECK/PELVIS (midpoint of projections != projection of midpoint, with 62 deg parallax): REFUTED as negligible. Triangulating NECK the shipped way (skeleton.py:170-174, 2D midpoints per view) vs triangulating both shoulders and taking the 3D midpoint differs by 0.254 mm median / 0.396 mm max = 0.22% of body height; PELVIS 0.078 mm / 0.108 mm = 0.07%. Torso bone (PELVIS-NECK) median 41.08 mm and CV 4.85% shipped vs 41.19 mm and 4.82% from 3D midpoints. The shoulder span (33 mm) is tiny against the 0.55 m depth, so the effect is second-order. Interestingly the derived points have LOWER epipolar error than their parents (NECK 1.51 px vs 3.86 px; PELVIS 4.78 vs 6.70) because averaging two keypoints averages down their independent noise. The real NECK/PELVIS problem is definitional, not perspective — see finding 2.

H4 (detector lock-on / velocity outliers / gates firing): REFUTED as a failure source. 38 left and 35 right frame-to-frame jumps exceed 4x the median 2D velocity, but they are frame-synchronous and hit ALL 15 joints at once in BOTH cameras at 0019->0020 and 0020->0021 (and the LEFT_WRIST jumps at 0007->0008, 0012->0013, 0013->0014 appear in both views too) — that is the mannequin actually being re-posed between shots, not the detector losing track. Scores do not drop across those transitions (0.85 -> 0.85, 0.87 -> 0.90). No frame shows the detector locked onto the gooseneck or a hand: YOLOX returns exactly 1 box on every frame tested, and every stored keypoint set is a coherent skeleton in the overlays. The in-image rejection (rtmpose.py:65-68) never fires (closest approach to an edge: 621.6 px left, 67.9 px right) and kpt_thr=0.2 fires on 2 of 780 observations.

Detection is deterministic and the stored 2D is genuine RTMPose output: re-running the full YOLOX+RTMPose pipeline on 3 frames x 2 cameras reproduces the stored kp2d with median difference 0.00 px (max 2.78 px on one joint of one frame). So none of the above is an artefact of a stale or hand-edited project file.

Right-camera intrinsics are NOT wrong: the CONTEXT brief states right K = [[4080,0,1536],[0,4080,2040]], but calibration/right_intrinsics.json actually holds K = [[2048,0,768],[0,2048,1024]] with image_size [1536,2048], which is the correct f=max(w,h) assumption for that sensor. The asymmetry is real (finding 7) but it is a hardware/optics fact, not a calibration bug.

NOTES: METHOD / VALIDITY

1. Nothing in the repository was modified. All scripts and outputs live in /tmp/claude-1000/-media-athena-hd3-Projects-pose3d-tool/0fcb8c9e-52c1-483d-9e0a-80896b078fa5/scratchpad/find-detection/ (red_dots.py, offset_stats.py, bone_frame_offset.py, bone_frame_fix.py, hybrid_retriangulate.py, impact3d.py, detector_probe.py, halpe_vs_coco.py, h4_scores.py, ui_feedback.py, midpoint_bias.py, plus dots.json / offsets.md / offset_stats.json / bone_frame_offset.json / bone_frame_fix.json / hybrid.json / impact3d.json / detector_probe.json / effective_res.json / halpe_vs_coco.json / h4_scores.json / ui_feedback.json / midpoint_bias.json and img/*.png). hybrid_retriangulate.py, impact3d.py, ui_feedback.py and midpoint_bias.py import the baseline metrics.py so every geometric number is comparable with the baseline report.

2. THE BIGGEST CAVEAT — what a "red dot" is. The dots are stuck on the mannequin's SURFACE, not at its rotation centres, and their placement is not consistent with any anatomical convention: the ankle dot sits on the shin/instep, the hip dot on the front of the thigh below the hip crease, and there are extra dots (sternum, navel, throat) with no canonical joint. So the 4.0 mm RTMPose-to-dot displacement is a discrepancy against THE CLIENT'S OWN VISUAL REFERENCE, not proof that RTMPose is anatomically wrong. It is still the right number for the client's complaint — they judge the overlay against the dots — but it must not be quoted as "RTMPose error vs ground truth". A true joint-centre reference would need the dot's radial offset fitted per joint.

3. Association reliability. Dot-to-joint association is greedy nearest-neighbour with mutual exclusion, top-3 candidates per joint, gated at 9% of figure height. With ~15 dots on a 776 px figure (spacing ~100 px) a mis-assignment to a neighbouring dot is possible, and the per-(camera, joint) sample counts are only 3-26. That noise is why the bias table could not be inverted usefully (see negative_results). The AGGREGATE finding — 12/12 distal joints biased proximally, median 8-11% of bone length — is far more robust than any single cell of the table.

4. The systematic/random split (2.42% vs 2.66% of figure height) is not a clean bias/noise decomposition: the "random" half mixes genuine pose-dependent RTMPose bias, association error, AND the real geometric motion of a surface marker as the limb rotates. The bbox-jitter probe pins RTMPose's own reproducibility at only 0.22-0.49% of figure height, so at most a fifth of the "random" half is detector jitter.

5. All detector re-runs used the exact shipped configuration — models.resolve("balanced", feet=False), i.e. yolox_m_8xb8-300e_humanart-c2c7a14a.onnx + rtmpose-m_simcc-body7_pt-body7_420e-256x192-e48f03d0_20230504.onnx from ~/.cache/rtmlib — via models.TwoStageDetector, the same class pose3d/detect/rtmpose.py:38 builds. The Halpe comparison drove both pose models from the IDENTICAL YOLOX box so only the pose head differs. Inference was limited to 3 frames x 2 cameras per probe (~6 minutes total); everything else used the stored kp2d.

6. Sample size. One take, 26 frames, 0 user corrections. Per-(camera, joint) offsets rest on 3-26 samples; the detector probes on 3 frames; the Halpe comparison on 3 frames. The direction and order of magnitude of findings 1 and 2 are solid (consistent across both cameras and all frames tested); the exact per-joint numbers are not.

7. Findings 3 and 4 are UI-layer, not detection, but they arrived through this dimension because they determine what "the character does not follow the keypoints" LOOKS like to the client. They are cheap and independent of every other auditor's dimension; whoever synthesises should not let them be crowded out by the geometry findings.

8. Cross-dimension note for synthesis: finding 1's 4.0 mm sits alongside the baseline's 6.2 mm smoother contribution and 1.8 mm retarget error. These are independent error sources that add — detection is roughly the second-largest single contributor measured so far, and unlike the smoother it cannot be fixed downstream because it is already in the measurement. Finding 2 (feet=True) is the cheapest large win in this dimension: one keyword argument plus a 55 MB checkpoint, removing a 12% of body height head offset the baseline had written off as definitional.


## auditor (categories ['calibration-extrinsics', 'calibration-intrinsics', 'cross-view-validation']) baseline_numbers: {"cameras":{"left":{"size":[3072,4080],"model":"Pixel 10 Pro","f35":24.0,"FocalLength_mm":6.9,"DigitalZoomRatio":1.26,"Orientation":1,"SubjectDistance_m":{"median":0.470,"min":0.439,"max":0.584},"focal_from_exif_px":2832.97,"shipped_f_px":4080.0},"right":{"size":[1536,2048],"exif":"none (JFIF only)","focal_from_exif_px":null,"shipped_f_px":2048.0}},"f_left_estimates_px":{"exif_alone":2832.97,"exif_x_zoom_1.26":3569.5,"BA_all_tags":3457.2,"BA_no_tag13":3574.3,"BA_tags_14_15":3570.1,"BA_tags_14_17":3452.5,"BA_profile_1sigma":[3400,3500],"BA_bootstrap":{"median":3466.0,"sd":131.3,"p10":3382,"p90":3497},"homography_IAC_median":{"f_norm":3389.6,"f_orth":3795.0},"AF_subject_distance_fixed_point":3383,"shipped":4080.0},"f_right_estimates_px":{"BA_all_tags":2118.5,"BA_no_tag13":1991.2,"BA_tags_14_15":1995.3,"BA_tags_14_17":1787.1,"bootstrap":{"median":2055.9,"sd":144.0},"homography_IAC_median":{"f_norm":1771.1,"f_orth":1936.0},"shipped":2048.0},"focal_ratio_fl_over_fr":{"shipped":1.992,"BA_all_tags":1.632,"BA_no_tag13":1.795,"F_of_body_points":[1.76,1.87],"epipolar_grid_min":1.79},"body_epipolar_median_px":{"noise_floor_bestfit_F":3.80,"F_lmeds":4.06,"F_ransac8px":4.06,"shipped_4080_2048":4.91,"single_3570_1790":6.43,"single_3457_2119":10.26,"single_2833_2048":26.55,"single_2833_1420":20.35,"single_2833_1200":16.97,"BA_4080_2048":6.05,"BA_3574_1991":9.98,"BA_3457_2119":13.13},"sweep_single_tag_matchedFOV":{"2500":{"epi":30.13,"boneCV":8.76,"sym":6.01,"span_mm":138.2,"dL_m":0.361},"2833":{"epi":20.35,"boneCV":6.59,"sym":5.39,"span_mm":136.1,"dL_m":0.406},"3200":{"epi":11.89,"boneCV":5.26,"sym":4.68,"span_mm":135.1,"dL_m":0.451},"3570":{"epi":6.43,"boneCV":5.19,"sym":4.00,"span_mm":132.1,"dL_m":0.495},"4080":{"epi":4.88,"boneCV":5.27,"sym":3.15,"span_mm":127.7,"dL_m":0.556},"4600":{"epi":5.94,"boneCV":6.07,"sym":3.28,"span_mm":124.2,"dL_m":0.619}},"sweep_BA_matchedFOV_epi_px":{"2500":29.09,"2833":22.07,"3200":15.01,"3570":8.41,"3600":7.96,"4080":6.03,"4600":8.14},"BA_profile_cost_vs_f_left":{"2833":3867.24,"3000":3860.78,"3200":3844.86,"3300":3837.10,"3400":3832.20,"3457":3831.37,"3500":3831.87,"3600":3837.32,"3800":3868.18,"4080":3957.62,"4400":4118.86},"BA_corner_rms_px":{"all_tags":{"left":4.76,"right":1.11},"no_tag13":{"left":3.96,"right":1.09},"tags_14_15":{"left":3.77,"right":1.09},"free_pp":{"left":4.72,"right":1.11},"free_k1":{"left":4.74,"right":1.16}},"per_tag_IPPE_rms_px_left":{"f4080":{"13":3.34,"14":0.80,"15":1.19,"17":0.40},"f3457":{"13":3.18,"14":0.49,"15":0.64,"17":0.44}},"shape_impact_vs_shipped_span_127.7mm":{"3800":{"mm":0.29,"pct":0.22,"ang_med_deg":0.42,"ang_max_deg":1.70},"3570":{"mm":0.56,"pct":0.44,"ang_med_deg":0.80,"ang_max_deg":3.31},"3457":{"mm":0.71,"pct":0.56,"ang_med_deg":1.01,"ang_max_deg":4.18},"3200":{"mm":1.09,"pct":0.85,"ang_med_deg":1.52,"ang_max_deg":6.39},"2833":{"mm":1.77,"pct":1.39,"ang_med_deg":2.45,"ang_max_deg":10.11}},"retarget_pct_height":{"2833":2.23,"3570":1.74,"4080":1.78},"k1_sweep_epipolar_px":{"-0.08":7.58,"-0.05":6.39,"-0.03":6.03,"-0.01":5.28,"0.00":4.91,"+0.01":4.77,"+0.03":4.28,"+0.05":4.47,"+0.08":5.07},"pp_offset_sweep_left_epipolar_px":{"-200":23.37,"-100":11.77,"-50":6.31,"0":4.91,"+50":5.82,"+100":9.89,"+200":19.84},"radii":{"left_subject_median_px":476.2,"left_subject_max_px":1079,"left_subject_pct_halfdiag":19,"left_tags_pct_halfdiag":[59,76],"right_subject_median_px":325.0,"right_subject_pct_halfdiag":25,"right_tags_pct_halfdiag":[67,77]},"distortion_displacement_px_at_k1_0.03":{"left_subject_median":0.27,"left_subject_max":3.15,"left_tags_median":10.87,"right_subject_median":0.23,"right_tags_median":4.99},"relpose_delta_vs_shipped_deg":{"BA_4080_2048":{"dR":0.76,"dt":0.63},"BA_3457_2119":{"dR":3.80,"dt":7.74},"single_3457_2119":{"dR":3.06,"dt":8.22}},"epipolar_gate":{"threshold_px":71.5,"pct_left_diag":1.4,"pct_right_diag":2.8,"pairs_over":0,"n_pairs":388},"tag_visibility":{"left":{"13":26,"14":26,"15":6,"17":26},"right":{"14":11,"15":2}},"intrinsics_source_field":{"left_file":"missing","left_loaded":"measured","left_looks_assumed":true,"right_file":"missing","right_loaded":"measured","right_looks_assumed":true}}

NEGATIVE: REFUTED / NULL RESULTS — do not re-open these.

(1) H1 as stated is WRONG IN BOTH DIRECTIONS. f = max(w,h) = 4080 is NOT "~44% longer than the physical focal"; it is +14% to +18% longer (true f_left = 3450-3575 px from five independent estimators). The 44% figure comes from comparing against focal_from_exif's 2833 px, which is itself 21% too SHORT because it ignores DigitalZoomRatio = 1.26 present in every client JPEG. Right camera: f = max(w,h) = 2048 px is right within ~3% (BA 1991-2119, bootstrap 2056 +/- 144, homography IAC 1771-1936); it is a different, EXIF-less 1536x2048 device.

(2) H2 REFUTED. A joint multi-tag/multi-frame bundle adjustment does NOT make the physically-right focal win on body epipolar error. With single-tag extrinsics: f_l = 2500/2833/3200/3570/3600/4080/4600 (right camera at matched FOV) gives 30.13/20.35/11.89/6.43/6.20/4.88/5.94 px median body epipolar. With BA extrinsics, the same sweep gives 29.09/22.07/15.01/8.41/7.96/6.03/8.14 px. The shipped f = (4080,2048) with single-tag PnP is the best of everything I tried (4.91 px) and the BA is uniformly worse than single-tag PnP at the same focal (6.05 vs 4.91 px at f=4080/2048), because the bundle is contaminated by tag 13 (see findings) and because IPPE PnP absorbs a focal error into the tag pose so a focal that is wrong the same way in both cameras largely cancels out of F. Bone-length CV and L/R symmetry are essentially flat across the whole plausible focal range (CV 5.19-5.27%, symmetry 3.13-4.00% for f_l in 3200-4600) and only degrade below f_l ~ 3000 (CV 8.76%, symmetry 6.01% at f_l = 2500). Epipolar error is therefore not a usable objective for absolute focal recovery in this rig; the tag bundle's own cost function is.

(3) H3(a) is essentially a NULL RESULT at this geometry. Single 5 cm tags viewed at low tilt give a hopelessly ill-conditioned image-of-the-absolute-conic constraint: of 84 left (frame, tag) solves only 28 yield a positive f from the orthogonality constraint (median 3795 px, p10-p90 2388-11035) and 82 from the equal-norm constraint (median 3390 px, p10-p90 2205-6267); the pooled least-squares over all left homographies returns f = 5612 px and the pooled right solve returns a NEGATIVE b = 1/f^2 (no real focal). Only the MEDIAN of the per-tag estimates is informative, and only as corroboration. H3(b) (f free inside the bundle) is the part that works: it converges to the same optimum from initial focals of 2833, 3570 and 4080 (f_left 3457.2 every time, agreeing to 0.1 px), 1-sigma band 3400-3500 from the cost profile, 3466 +/- 131 from a frame bootstrap.

(4) Self-calibration from the fundamental matrix of the body correspondences is DEGENERATE here. Bougnoux's closed form gives negative f^2 for both cameras (f_l^2 = -2.31e6, f_r^2 = -7.44e5) and 0/200 bootstrap resamples give a positive f^2 for either camera. The essential-matrix singular-value residual |s1-s2|/mean is monotonically increasing in f over 2200-5600 px with no interior minimum: it constrains only the RATIO f_left/f_right to 1.76-1.87 and says nothing about the absolute scale. Do not build a focal estimator on it.

(5) H4 REFUTED for the subject. The mannequin's keypoints sit at only 19% (left) / 25% (right) of the half-diagonal, so a plausible residual k1 = +/-0.03 displaces them by 0.27 px median / 3.15 px max (left) and 0.23 px / 2.78 px (right). Zero distortion costs the pose essentially nothing. It is NOT negligible for the tags (10.9 px median displacement at k1 = 0.03, since they sit at 59-77% of the half-diagonal), so distortion can only reach the reconstruction through the extrinsics.

(6) H6 REFUTED — there is no orientation bug anywhere. EXIF Orientation = 1 on all left images; cv2.imread returns (4080,3072,3) left and (2048,1536,3) right with and without IMREAD_IGNORE_ORIENTATION (identical); PIL im.size agrees (3072,4080) and (1536,2048); the saved intr.image_size matches both; the stored kp2d live inside each image's own frame (left x 1582-2450 of 3072, y 1670-2975 of 4080; right x 68-788 of 1536, y 848-1540 of 2048); ArUco corners likewise. focal_from_exif is orientation-immune anyway because it uses hypot(w,h).

(7) THE BOUND THAT MATTERS: intrinsics cannot be the client's problem on this take. The best-fit fundamental matrix over the same 388 body correspondences — 7 free parameters, no calibration at all — achieves 3.80 px median Sampson error (LMEDS 4.06, RANSAC@8px 4.06). The shipped rig achieves 4.91 px. So EVERY calibration error combined (focal, principal point, distortion, extrinsics) accounts for at most 1.11 px of the 4.91 px cross-view disagreement; the other 3.80 px is RTMPose 2D noise. Correspondingly, replacing the shipped focal with the measured true one changes the reconstruction by 0.56 mm per frame after a best-fit similarity (0.44% of the 127.7 mm subject span), 0.80 deg median / 3.31 deg max joint-angle change, and moves the character retarget error from 1.78% to 1.74% of body height. Even the extreme f_l = 2833 only reaches 1.77 mm (1.39% of span) and 2.45 deg median. For comparison the baseline audit measured the EMA smoother displacing joints by 5.27% of body height on average and 21.7% at worst. Intrinsic calibration is a correctness/metric-scale issue, not the cause of "the character does not follow the keypoints".

(8) The focal cannot explain the absolute-scale discrepancy (reconstruction says the mannequin spans 128 mm, the client describes a ~20 cm figure). Across f_l = 2500 to 4600 the reconstructed subject span only moves 138.2 -> 124.2 mm, and the wrong direction to close the gap: world scale is pinned by marker_length = 0.05 m, not by f. Look at the tag size / marker_length assumption instead.


NOTES: SCOPE AND METHOD
- Nothing in the repository was modified. All scripts and outputs live in /tmp/claude-1000/-media-athena-hd3-Projects-pose3d-tool/0fcb8c9e-52c1-483d-9e0a-80896b078fa5/scratchpad/find-intrinsics/: exif_dump.py + exif.json (EXIF of all 52 images), detect_tags.py + tags.json (ArUco DICT_6X6_250 corners for all 52 images, 3.1 s), common.py (rig builders, the tag bundle adjuster, evaluate_rig), focal_sweep.py + focal_sweep.json (H2: the required f -> metrics table, including a 13x13 (f_l,f_r) epipolar grid), self_calib_f.py + self_calib_f.json (H3 homography IAC, BA with free f, F-matrix/Bougnoux, EXIF-autofocus cross-check), ba_profile.py + ba_profile.json (focal uncertainty by cost profile and frame bootstrap, pp/k1 variants, H4 distortion radii, head-to-head rigs), arbiter.py + arbiter.json (noise floor of the body correspondences, k1 and pp sweeps, per-tag IPPE residuals), tag_subset.py + tag_subset.json (BA per tag subset, H6 orientation checks, source-field check), shape_impact.py + shape_impact.json (Procrustes/joint-angle/retarget impact of the focal), summary.json (all headline numbers in one file).
- All metrics reuse the parent's /scratchpad/baseline/metrics.py (M.triangulate, M.bone_length_stats, M.retarget_error) so the numbers are comparable with the baseline. The rigs I build are transient objects; the on-disk calibration was never touched.
- The bundle adjuster is 2 static camera poses + the poses of the non-world tags relative to tag 14 (world frame), residual = every detected marker corner in every frame of both cameras, scipy least_squares LM, 97 tag views / 776 residuals / 32-34 parameters. Optional free parameters: log f per camera, principal-point offset per camera, k1 per camera.

CAVEATS ON MY OWN NUMBERS
1. The DigitalZoomRatio interpretation (true f = f35-derived focal x 1.26) is an INFERENCE. What I measured is that the tag bundle without tag 13 returns f_left = 3574.3 px and with tags 14+15 returns 3570.1 px, and that 2832.97 x 1.26 = 3569.5. The agreement to 0.2% is striking but I could not find explicit crop metadata in the JPEG (no XMP crop/zoom fields; only HDR+ and hdr-gain-map entries). Google could in principle be reporting f35 = 24 for the zoomed frame, in which case the coincidence is accidental and the true focal is simply ~3450-3575 measured. Either way the actionable conclusion is the same: the focal is ~3500, not 2833 and not 4080. The 21%-short claim about focal_from_exif rests on the measurement, not on the zoom story.
2. The frame bootstrap (f_left 3466 +/- 131) UNDERSTATES the uncertainty: the cameras and tags are static, so the 26 frames are not independent samples and any systematic corner-detection bias is shared by all of them. The BA cost profile band (3400-3500) is likewise a formal 1-sigma with no allowance for model error, and the left corner rms of 3.96-4.76 px proves there IS model error. Treat "f_left is in 3400-3600" as the honest statement.
3. The EXIF SubjectDistance cross-check assumes the Pixel's autofocus locked on the mannequin rather than the backdrop or the clamp, and that its reported distance is accurate to ~10%. Its 26 values range 0.439-0.584 m with median 0.470. It is corroboration, not a measurement I would quote alone.
4. The (f_l,f_r) epipolar grid, the F-matrix ratio and the tag bundle disagree slightly about the focal RATIO (1.79, 1.76-1.87, 1.63-1.80). I did not resolve that; all three agree the shipped 1.99 is high.
5. My body-epipolar comparisons across rigs all use the STORED kp2d, which the baseline established is already post-validate_cross_view. Re-running the gate drops nothing more, so no rig in my sweep is being scored on a different keypoint set.
6. The 3.80 px "noise floor" is the median Sampson error of a 7-parameter F fitted to the very 388 points it is scored on, so it is optimistic (in-sample). That makes the bound conservative in the right direction: the true calibration-attributable share of the 4.91 px is at most 1.11 px, probably less.
7. All of this is one 26-frame take with one phone and one webcam. The right camera's focal is only weakly observable here (it sees tag 14 in 11 frames and tag 15 in 2), which is why its BA estimate swings 1787-2119 depending on the tag subset. A second take, or the client photographing a checkerboard once with each device, would settle it in minutes.
8. Overlap with other auditors: finding 5 (tag 13) and finding 6 (epipolar threshold) touch the extrinsics and cross-view dimensions. I report them because they were discovered through the intrinsics experiments (they are what breaks the tag-based focal estimator); defer to those auditors on the extrinsics consequences.


# MERGE NOTES
PATH ABBREVIATION: <SP> = /tmp/claude-1000/-media-athena-hd3-Projects-pose3d-tool/0fcb8c9e-52c1-483d-9e0a-80896b078fa5/scratchpad. Expand it in every artifacts field. All auditors worked read-only against workspace/pose3d_projects/Imported_Session with the stored kp2d; the repository was not modified.

MERGES PERFORMED (54 input findings -> 45; 9 merges, nothing dropped):
- F07 = pipeline "gauge measures RAW triangulation" + tests-ui "gauge measures raw and cannot detect a wrong rig" (both pose3d/ui/model.py:187). Severity taken as critical (the higher of major/critical). Evidence pooled: 5.11/2.78 px raw vs 21.65/17.18 px fitted, gauge 52.3% vs 6.2%, 15 deg rig error moves it only 52.3->49.4%.
- F10 = pipeline "zero-px drag jumps the pose" + tests-ui "correcting one keypoint re-fits with a different algorithm" (both model.py:160). Includes tests-ui's extra mechanism: self._bone_lengths cached at the first drag and never refreshed.
- F15 = extrinsics "world up 89.3 deg off gravity, no gravity reference" + retarget "no trustworthy gravity reference; sequence_up erases lean" (resolve.py:143 and orient.py:78; most specific line kept = orient.py:78, where sequence_up lives). Both proxy sets preserved; confidence blended to 0.65 because the two auditors' proxy angles differ (9.0-11.3 vs 9.7-22.2 deg) and neither has a real gravity measurement.
- F16 = detection "project has no head2d/head3d, no re-detect path" + tests-ui "import dialog's private loop drops head_xy" (rtmpose.py:56 and import_dialog.py:204; kept import_dialog.py:204, the causal line).
- F20 = intrinsics "two cameras are different devices, EXIF applies to only one" + extrinsics "cameras differ in resolution and the pipeline never uses that" (resolve.py:116 and :110; kept :116). Severity major.
- F24 = detection "UI paints 73% of joints red" + tests-ui "accuracy pct and timeline bands are absolute pixels" (panels.py:33 and :37; kept :33). tests-ui's per-camera-averaging half of that finding is NOT folded in here — it is a distinct mechanism kept as F25 (model.py:180).
- F29 = intrinsics "Intrinsics.load defaults source='measured'" + the same sub-claim inside the tests-ui provenance finding (intrinsics.py:58).
- F30 = extrinsics "extrinsics.json records no provenance" + tests-ui "calibration provenance and quality never persisted or shown" (both resolve.py:44). Severity major.
- F31 = retarget "Character returns None silently and View3D swallows it" + tests-ui "three blanket excepts in the 3D view and rig loader" (view3d.py:205/99 and app.py:95; kept view3d.py:99). Severity major.
- F36 = extrinsics "the 71.5 px gate is 14.6x the observed median" + intrinsics "epipolar_threshold scales with the LEFT diagonal only" (pipeline.py:53 and :59). Two mechanisms in one function with a single combined fix; kept as one entry with both labelled.
KEPT SEPARATE despite shared files: F03 vs F04 vs F16 (head — clamp fudge, wrong model, dropped keypoints are three different mechanisms); F02 vs F12 vs F43 (character.py roll — general roll, hip-vs-shoulder twist, antipodal singularity); F01 vs F09 vs F14 (smoothing — lag, NaN resurrection, ordering); F32 vs F45 (testing — geometric end-to-end fixture vs detector accuracy fixture); F22 vs F23 (tag 13 non-planarity vs IPPE branch selection); F24 vs F25 (band thresholds vs cross-camera averaging).

CROSS-DIMENSION ERROR BUDGET (from the auditors' own bounds, for prioritisation): EMA smoother 5.27% of body height mean / 21.78% max (F01) — 98% of the deviation between the shipped pose and the measurement; detector-vs-red-dot 3.4% (F05); uniform-scale retarget floor 1.4% median, ~4% at neck/right knee (F08); bone-fit systematic 1.2-1.4% (F34); whole extrinsics dimension bounded at ~0.15% (0.13% per degree, rig uncertain at <=1.1 deg, F38); whole intrinsics dimension bounded at <=1.11 px of the 4.91 px epipolar median against a 3.80 px best-fit-F noise floor, i.e. 0.44% of subject span (F21). Twist/roll (F02), head clamp (F03) and foot bob (F11) are large ANGULAR errors invisible to every positional metric in the repo.

NEGATIVE RESULTS — REFUTED, DO NOT RE-OPEN.

EXTRINSICS. H1 the shipped rig sits on the planar-pose ambiguity: REFUTED. It is tag 14, frame 0001, IPPE solution 0, both cameras (matched to 0.0000 deg / 0.0000 mm), but the second solution's reprojection error is 11.92x the first (left) and 9.27x (right), the branches differing by 63.7 / 67.5 deg; over the 11 frames where the right camera also sees tag 14, frame 0001's pose is within 1.32 / 1.33 deg of every other frame's and the camera centre moves at most 12.9 / 14.0 mm. Frame lottery costs 1.13 deg = 0.15% of height. H2 pooling materially improves the rig: REFUTED (numbers in F38); BA at the EXIF focal stays self-consistent on TAGS (rms 3.26 px, tag-15 holdout 49.36 mm) but the body keypoints reject it (epipolar 22.73 px median, p90 43.75, bone CV 7.41%, asymmetry 5.63%, retarget 2.48%, 1 observation dropped). Do NOT evaluate "EXIF K on the shipped extrinsics" — reprojection 145.5/100.2 px, height 0.156 m, because those extrinsics are PnP-derived at the assumed K. H5 recover the rig from body keypoints: REFUTED (F42 numbers). NOT a defect: camera motion — per-tag camera-centre std 2.8/2.0/1.1 mm (left tag 14), 3.4/2.7/2.0 (left tag 17), 3.6/5.4/5.0 (right tag 14); tag-distance std 0.8-2.5 mm. METHOD TRAP: metrics.py --calib does NOT change the 3D (source 'raw' returns take['pose3d'] verbatim, metrics.py:184; 'refit'/'refit-nosmooth' fit that same stored pose3d), so running the CLI across calibration folders gives identical bone CV / symmetry / height / retarget / bend-plane columns — an artefact. Re-triangulate explicitly with M.triangulate(kp2d, rig) (eval_rigs.py).

INTRINSICS. f = max(w,h) = 4080 is NOT ~44% longer than the physical focal; it is +14 to +18% (true f_left 3450-3575 from five estimators). The 44% figure came from comparing against focal_from_exif's 2833, itself 21% short (F21). Right camera f = 2048 is right within ~3%. H2 a bundle makes the physically-right focal win on body epipolar: REFUTED — with single-tag extrinsics, f_l = 2500/2833/3200/3570/3600/4080/4600 gives 30.13/20.35/11.89/6.43/6.20/4.88/5.94 px; with BA extrinsics 29.09/22.07/15.01/8.41/7.96/6.03/8.14; the shipped (4080,2048) with single-tag PnP is the best of everything tried (4.91 px). Bone CV and symmetry are flat over f_l 3200-4600 (CV 5.19-5.27%, symmetry 3.13-4.00%) and only degrade below ~3000 (CV 8.76%, symmetry 6.01% at 2500). Homography IAC self-calibration is a near-null result (28 of 84 left solves give a positive f, median 3795 px, p10-p90 2388-11035; equal-norm form 82 solves, median 3390, p10-p90 2205-6267; pooled left least-squares returns 5612 px, pooled right returns a NEGATIVE b = 1/f^2). Bougnoux from the body F-matrix is DEGENERATE (f_l^2 = -2.31e6, f_r^2 = -7.44e5; 0/200 bootstrap resamples positive); the essential-matrix singular-value residual is monotone in f over 2200-5600 and constrains only the ratio 1.76-1.87 — do not build a focal estimator on it. H6 image-orientation bug: REFUTED (EXIF Orientation = 1; cv2.imread returns (4080,3072,3) and (2048,1536,3) with and without IMREAD_IGNORE_ORIENTATION; PIL agrees; stored kp2d lie inside each frame — left x 1582-2450 of 3072, y 1670-2975 of 4080; right x 68-788 of 1536, y 848-1540 of 2048; focal_from_exif is orientation-immune via hypot). THE BOUND: a best-fit F over the same 388 correspondences — 7 parameters, no calibration — achieves 3.80 px median Sampson (LMEDS 4.06, RANSAC@8px 4.06) vs the shipped rig's 4.91, so ALL calibration error combined is at most 1.11 px; the rest is RTMPose noise. The focal cannot explain the metric-scale gap (span moves only 138.2 -> 124.2 mm across f_l 2500-4600, the wrong direction) — look at marker_length (F17).

PIPELINE. H5 derived NECK/PELVIS perspective bias: REAL BUT NEGLIGIBLE — NECK vs the 3D midpoint of the triangulated shoulders 0.254 mm median / 0.396 max (0.22% of height, 0.8% of the 31.97 mm shoulder span); PELVIS 0.078 / 0.108 mm (0.066%, 0.47% of the 16.63 mm hip span); stored 2D verified to be the exact pixel midpoint (max deviation 0.0000 px). Cause: shoulders differ in depth by only 7.91 mm median / 23.58 max at 0.559 m = 1.41% relative depth spread. Torso bone 41.08 mm / CV 4.85% shipped vs 41.19 / 4.82% from 3D midpoints. The derived points actually have LOWER epipolar error than their parents (NECK 1.51 vs 3.86 px; PELVIS 4.78 vs 6.70). H8 better triangulators: REFUTED — midpoint-of-common-perpendicular vs cv2.triangulatePoints differs by 0.005 mm median / 0.143 max (bone CV 5.266 vs 5.272%; reproj 5.106/2.785 vs 5.107/2.783 px); Hartley-Sturm optimal correction differs by 0.373 mm median / 2.323 max and makes bone CV WORSE (5.452%). H9a root drift in the fit: REFUTED — PELVIS moves 0.76 mm median / 2.92 max, all-joint median 0.59 mm, centroid shift 0.00 mm. H9b bone_weight=5.0 too soft: REFUTED — every bone's median length matches its target to within 0.010 mm (worst neck-head -0.010), bone CV 0.142% median / 0.367% max. H7 missing ray-constrained single-view placement: CONFIRMED as a code gap but inert — 2 of 390 observations are single-view (frame 0012 RIGHT_KNEE, frame 0021 LEFT_ANKLE, both left-only), 0 in neither view; the gate has 2.43x headroom and re-running drops 0 three times running. Fix the smoother (F09), not the gate. Detector confidence is not a gating problem in derive_joints either (left median 0.88, 0.0% below 0.35, 1% below 0.60; right median 0.82, 0.5% / 2.1%; no joint's median below 0.716).

RETARGET. The bend-plane metric is NOT how to see the roll defect: fitted3d's bend-plane angle is 3.6 deg median / 14.9 max because both limb bones aim independently at their own captured joints; the defect is the 13.6-53.6 deg twist ABOUT each bone axis. Do not conclude limb orientation is fine from 3.6 deg. H7 view re-centring caused by dropouts: REFUTED (fitted3d has no NaN in any of the 26 frames, so the shift is exactly 0.0 m). H6 Character exceptions: REFUTED (130 sparse variants, 0 exceptions, 52 (None,None,None) returns all from patterns deleting PELVIS + both hips; the real take posed 26/26). H8 the uniform scale is mis-fitted: REFUTED (scale 113.62; posed canonical height 1.052x the subject's, mesh height 1.201x with the rig's own mesh-to-joint ratio 1.097; removing the two definitionally-mismatched bones moves the scale to 114.55 / 114.34, under 1%; the leg 2x weighting in _SCALE_WEIGHTS does apply — Joint is an IntEnum). The aim mechanism itself works: PELVIS lands at 0.00% error, shoulders ~1.0%, shoulder line correct to 0.2 deg median. Head error is NOT mostly definitional — only 4.6 of the 12.6 percentage points. Export/view ORIENTATION maths does match (blender_export.py:61 vs view3d.py:106; same fit_to_subject at blender_export.py:76); only root translation differs.

DETECTION. H2 effective resolution is the bottleneck: REFUTED — balanced = YOLOX-m (640,640) + RTMPose-m (192,256); on the real boxes the nose-to-ankle span occupies 119.0/128.5/140.0/148.5/158.0/159.0 of 256 model rows, so 1 model pixel = 2.54-6.62 original px = 0.63-0.84% of figure height in BOTH cameras, and SimCC's 2x split puts the quantisation floor near 0.4% of figure height (~0.4-0.5 mm); the measured discrepancy is 4.0 mm, 8-10x that. Tight-crop re-detection moves keypoints only 1.87-5.47 px median (0.27-0.90% of figure height, max 25.5 px); box perturbation at sigma=3%, 12 draws, moves them 1.26-2.79 px median (0.22-0.49%, max 15.2) — so the 2.66% "random" component is NOT detector jitter. STILL OPEN: mode="performance" would give (288,384), 1.5x resolution; its checkpoints were not in the local cache. H3 hybrid RTMPose-association + red-blob centroid as a drop-in: REFUTED — raw bone CV median/max 5.27/9.52% (RTMPose) vs 8.85/14.19% (snap both views) vs 10.79/29.44% (snap per-view); the dots are on the limb SURFACE so inter-dot distance genuinely changes with rotation. What DID improve on the same joint subset: epipolar 4.29 -> 3.59 px (-16%), reprojection 4.37/2.43 -> 3.67/2.06 px. Inverting the measured bias as a pose-adaptive correction: REFUTED at this measurement quality (alpha=1.0 full bone-frame: bone CV 5.27 -> 6.37%, max 9.52 -> 14.55%; along-only 5.22%; forearm asymmetry improves 6.72 -> 0.6-2.8% but upper arm degrades 0.48 -> 8.9-21.6%). H4 detector lock-on / velocity outliers: REFUTED — the 38 left and 35 right >4x-median jumps are frame-synchronous across ALL 15 joints in BOTH cameras at 0019->0020 and 0020->0021 (and LEFT_WRIST at 0007->0008, 0012->0013, 0013->0014 in both views): the mannequin was re-posed. Scores do not drop across them (0.85->0.85, 0.87->0.90); YOLOX returns exactly 1 box on every frame tested. Detection is deterministic and the stored 2D is genuine RTMPose output (re-run reproduces it to 0.00 px median, max 2.78 px on one joint).

TESTS-UI. "The signals can read GREEN while the 3D is wrong" is REFUTED in the green direction — the gauge is SATURATED RED (52.3% median, 26/26 frames red, Panoptic GT reference also red at 44.7%) and rig-blind, not falsely green; frame it as uninformative. "No report of which tag/frame calibrated" is refuted at the message level (resolve.py:148-149 builds it, import_dialog.py:225 shows it) — the defect is that it is never persisted (F30). "check_rig stays silent" is refuted: it fires three warnings on this rig. io_project round-trip does NOT lose fitted3d / head2d / head3d / corrected (io_project.py:102-161, tested at test_io_project.py:28-55 and test_head_keypoints.py:45-58, :61-79). validate_cross_view re-run on the STORED client 2D drops 0 further observations, so F06 is a demonstrated mechanism, not a claim this take is already damaged (the original detections are gone, so that cannot be established either way). The suite is fully green and fast: 189 collected, 183 passed, 6 skipped, 4.88 s, exit 0, no flakiness. The Panoptic validators work; they are simply not collected (pyproject sets testpaths=['tests'] with default python_files=test_*.py) — renaming suffices.

FACT CORRECTION TO THE SHARED BRIEF (three auditors hit it independently): the two cameras are NOT both 3072x4080 with K=[[4080,0,1536],[0,4080,2040]]. LEFT is 3072x4080 (Pixel 10 Pro, EXIF FL35=24 mm, DigitalZoomRatio 1.26); RIGHT is 1536x2048 with NO EXIF at all and right_intrinsics.json holds K=[[2048,0,768],[0,2048,1024]]. Both are still f = max(w,h) guesses (looks_assumed True for both). Any experiment assuming a common K, a symmetric rig, a shared epipolar threshold, an averaged pixel error or a single EXIF focal correction is wrong on this take; "f=2833 for both" is not a meaningful EXIF hypothesis.

METHOD CAVEATS THAT LIMIT THE NUMBERS ABOVE: (1) one take, 26 frames, 0 user corrections — per-joint medians rest on 25-26 samples and every "max" is one frame; the live-edit findings come from a Qt-free reimplementation of ProjectModel._resolve_joint (probe2.py), i.e. the code path and its arithmetic, not on-screen pixels. (2) HEIGHT = 0.1178 m (z-extent of the de-tilted pose) throughout, so all "% of height" figures are comparable. (3) The red dots are SURFACE markers with no anatomical convention (the ankle dot is on the shin/instep, the hip dot on the front of the thigh; there are extra sternum/navel/throat dots) — F05's 4.0 mm is a discrepancy against the CLIENT'S visual reference, not against ground-truth joint centres; the aggregate 12/12-distal-joints-proximal result is far more robust than any single per-joint cell (3-26 samples, greedy association). (4) The tag-15 cross-view holdout rests on ONE square in ONE frame (0026): read it as "the rig is right to about half a percent", not as a precise figure. (5) The extrinsic perturbation study rotates only the RIGHT camera and averages 5 random axes, so 0.13% of height per degree is an average over axis directions at this geometry, not a worst case. (6) The DigitalZoomRatio interpretation is an inference — no explicit crop metadata was found in the JPEG; what is measured is that the tag bundle without tag 13 returns 3574.3 px and 2832.97 x 1.26 = 3569.5. Either way the actionable conclusion is f_left ~3400-3600, not 2833 and not 4080. (7) The frame bootstrap (3466 +/- 131) understates uncertainty: static cameras and tags mean the 26 frames are not independent. (8) The focal sweep is coarse (0.1 steps, 49 BA runs) and uses the same body-epipolar criterion as commit f4c92f9 — it removes that comparison's circularity (extrinsics re-optimised per focal) but is not a fully independent confirmation. (9) F13's export claim is measured on the bone matrices handed to Blender plus a read of blender_job.py:468; Blender itself was not run. (10) matplotlib is absent from .venv, so the retarget renders (contact_front.png, contact_side.png) come from a hand-written PIL painter's-algorithm renderer reproducing View3D.set_pose's framing — read them: the body tracks well, the FEET and HANDS are the visibly wrong parts, and the no-nose-fudge variant visibly pitches the head down, which is why F03's fix is "unclamp and fix the reported point", not "aim at the nose".