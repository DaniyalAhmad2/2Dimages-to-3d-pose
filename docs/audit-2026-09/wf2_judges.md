# JUDGES


## Judge 1: best_index=2
- P0: total 39.5 (acc 9, sound 8, risk/effort 6, fit 8.5, verif 8)
  + Broadest coverage of what is actually visible: smoother (the measured 98%), roll, grounding, dead head feature, root motion, gravity, gauge. Best treatment of the single hardest technical point in the whole audit — F02's continuity problem: it correctly rejects the auditor's 15 deg straightness guard (the left elbow's minimum captured bend is 22.7 deg, above the guard), uses a continuous sin(bend) weight PLUS a stateless torso-based sign convention, sets an explicit ship gate (max consecutive roll change <= 15 deg), and specifies a fallback that preserves the hard constraint — a pure retarget.roll_references(poses3d, valid) precompute called identically by view3d and blender_export, so Character stays a pure function and view == export for free. Correctly refuses the refuted fixes: keeps the nose-pitch clamp for legacy (F03), refuses sole-horizontal feet (F11 refuter), refuses symmetrised fit targets, refuses the essential-matrix fallback. Labelled gap fill with a persisted per-(frame,joint) flag drawn hollow in both views — the only version of F09's fix that is actually honest. Phase 1 item 9's bbox-jitter noise-floor probe makes 'smoothing off' a measured decision rather than an assumption. Migration (version stamp + recompute-on-open + banner with the real numbers + Restore, never auto-save) is well designed. Halpe behind a measured gate table including the neck-Lshoulder CV regression. Client questions are the most decision-relevant of the three.
  - Scope: nine phases, several L, for one developer. Phase 4 prescribes a Blender interpolation rework — CONSTANT hold keys, Bezier ease, a quaternion hemisphere pass — on the assumption that the 772-frame timeline's in-betweens are defective, which is exactly the 'fix the reorder and call it the fix' failure mode F14 warns about; proposal 2 measured the delivered file and found holds bit-identical and in-between overshoot <= 3.80 deg with zero hemisphere flips, so a budgeted day on the repo's least-tested surface (all 5 export tests skip locally, API moved in Blender 5.x) buys nothing. Phase 2 item 8 ('hold that bone's LOCAL rotation from the previous posed frame' when an aim target is missing) puts temporal state inside _skin_matrices, contradicting the statelessness the same phase declares load-bearing; I confirmed view3d.set_pose is called per frame from main_window.py:450 on arbitrary timeline scrubs, so previous-frame state is order-dependent and can desync view from export. Verifiability has a forward dependency: Phase 1's tests/test_client_regression.py needs the committed fixture that Phase 9 delivers. The roll's decisive check is an explicit 'visual gate, not automated' contact-sheet review — honest, but it means the phase the client judges by eye has no CI number.
- P1: total 39 (acc 9, sound 7.5, risk/effort 6, fit 7.5, verif 9)
  + The sharpest evidence handling of the three, with several genuine refinements the audit itself did not reach. Phase C's policy dict (head='native', neck='derived', pelvis='derived') is the best Halpe treatment in the set: it takes the measured HEAD win (12.65% -> 3.93% of height, neck-head CV 9.53% -> 3.85%, the worst bone in the baseline) while avoiding the measured neck-Lshoulder regression (5.15% -> 8.13%), and justifies keeping the derived midpoints with the auditors' own numbers (derived NECK epipolar 1.51 px vs its parents' 3.86; midpoint construction penalty 0.25 mm). Phase D catches that the auditor's 'reject only if BOTH exceed' per-image gate is strictly MORE permissive than today (d_R max 33.8 px vs a 35.8 px allowance) — a real trap avoided. Phase E's focal ladder ships provenance without shipping a focal change (correct: the assumed focal is at or near the measured epipolar optimum) and guards against giving an EXIF focal to a camera with no EXIF. Phase F carries the correction that sequence_up removes only the MEAN lean and uses a clean binary sign test (NECK above ANKLE). Harness-first with three consumers (pytest, CLI, sidebar) so ship gates and the client's readout are literally the same code, and the 'this phase must be bit-identical on the fixture' invariance assertions for the robustness and gravity phases are the cheapest possible proof that a robustness change did not silently re-tune the reconstruction.
  - Phase G breaks the plan's own hard constraint. It carries per-bone roll across frames with hysteresis in a caller-owned RollState, justified by 'View3D and blender_export._character_bone_frames, which both iterate the take in order'. That premise is false: I confirmed main_window.py:450 calls view3d.set_pose(f.fitted3d, f.head3d) per frame on timeline change, in whatever order the user scrubs, while blender_export iterates once in order — so the same frame can produce different matrices in the view and the export, and the promised bit-for-bit parity test would not hold under scrubbing. This is in the phase the client judges by eye, and the fix (a per-take precompute of reference vectors) is exactly what proposal 0 specifies and this one does not. Phase G also ships an explicit foot rule (sole parallel to the ground, blended by ankle height) with an acceptance of sole tilt <= 10 deg median against today's 30-32 deg, after F11's refuter specifically measured that the shin genuinely tilts 16-86 deg on a rigid-footed mannequin and that forcing the sole horizontal trades pose fidelity for a datum the ankle rule already fixes. Phase A is L effort that moves no joint and re-bands the gauge before the smoother lands, so the client's readout changes twice for different reasons. Halpe lands mid-plan and moves reconstructed height +9.8%, invalidating every recorded threshold — handled by re-baselining in the same commit, but it is real churn in the middle of the sequence.
- P2: total 43 (acc 8, sound 8.5, risk/effort 8.5, fit 9, verif 9)
  + The only proposal that closed the audit's largest open gap by actually opening the deliverable, and its findings change what should be built. I verified its claims directly: assets/Imported_Session.bvh declares Frames: 772 at 30 fps for 26 poses; the hips translation is 0.000000 -0.131342 2.851750 on every motion line (F13 confirmed at the file level, not only in the matrices); data/demo_project/export/demo.bvh really is a null ROOT __0 with CHANNELS 0 and sibling joints carrying 6 channels each — the shape the silent blender_export fallback writes. Its measurement that the holds are bit-identical and the eased in-betweens overshoot by at most 3.80 deg with no hemisphere flips REFUTES the interpolation rework proposal 0 budgets a day for, and redirects Phase 3 onto what is measurably wrong: zero root motion, orphan IK helper bones (shin.L.001/shin.R.001) carrying up to 42.5% of rig height of translation, and the blanket excepts that let a missing asset ship a different retarget silently. Smallest, best-sequenced plan for one developer: six phases, three of them net deletions, ordered by measured effect per line touched, each independently shippable. Best test discipline: xfail(strict=True) on exactly the three assertions that fail today (reproj(fitted)/reproj(raw) <= 1.5, max|fitted-refit| <= 3% of height, helper-bone translation <= 1% of rig height) so they flip to green as the fix lands; thresholds set 10-15% above today's values, directly answering F32's flakiness critique; close_range_two_cam is asymmetric (4080@3072x4080 vs 2048@1536x2048), which is what makes the client's 2:1 rig testable at all. Its roll weight is better tuned than proposal 0's: at the measured 22.7 deg minimum bend, clip((bend-20)/20) gives w ~ 0.15 (proposal 0's sin-based endpoints give ~0.87), so it actually suppresses the 55.7 deg bend-normal jump the refuter found, and it stays stateless, preserving pose_bone_matrices as a pure function and therefore view == export by construction. Correctly declines every refuted fix and publishes an explicit NOT-removed list with reasons (IK stays as the occlusion path, _NOSE_PITCH stays for legacy, calibrate_checkerboard stays as the sanctioned optional override, the 71.5 px fraction stays until data justifies changing it). Phase 1b's compact migration (version stamp, recompute-on-open, detect_project honouring frame.corrected, import calling the canonical loop, one 'Re-detect face points only' menu action) is the whole of F16 plus the delivery mechanic in about four small edits.
  - Deletes smooth_temporal outright rather than defaulting it off, while its own client question 7 asks whether future takes will ever be video-rate — the capability is removed before the answer arrives, and rollback then costs a re-implementation instead of a checkbox. Its gap fill is symmetric and lag-free but only counted in FitReport, not flagged per (frame, joint) and drawn as interpolated, so F09's 'shown as if measured' complaint is only half answered. Defers Halpe-26 wholesale, which forfeits the measured HEAD 12.65% -> 3.93% and neck-head CV 9.53% -> 3.85% win that proposal 1 shows is separable from the NECK/PELVIS redefinition. Defers gravity entirely, leaving the delivered animation ~15 deg off the three-proxy consensus — defensible reasoning (the proxies disagree by 8-14 deg) but it is a visible defect left standing with not even the number recorded. The roll phase has no fallback if its own <= 15 deg continuity gate fails; rollback is 'set the weight to 0', i.e. back to today. Deleting focal_from_exif and its test discards F21's corrected knowledge (2833 px was 21% short because DigitalZoomRatio was ignored) in a plan that simultaneously asks the client for a checkerboard.
GRAFTS: GRAFT 1 (from proposal 1, Phase C) into a NEW gated phase in the winner, after Phase 4 and before Phase 5 — the Halpe head win, without the skeleton redefinition. Add skeleton.map_halpe26(kp, scores, head='native', neck='derived', pelvis='derived') as a policy dict rather than a model flag: take Halpe index 17 (skull vertex) for HEAD, keep NECK and PELVIS as the 2D midpoints. Evidence for the split is in the audit: native HEAD gives retarget 12.65% -> 3.93% of height (9.09% -> 2.92% with face points) and neck-head bone CV 9.53% -> 3.85% (the worst bone in the baseline), while native NECK regresses neck-Lshoulder CV 5.15% -> 8.13% and the derived midpoints are measurably better conditioned (derived NECK epipolar 1.51 px vs its parents' 3.86; construction penalty 0.25 mm at NECK, 0.08 at PELVIS). Carry proposal 1's two safety items with it: gate character.py:38 _NOSE_PITCH and _head_aim_target on a persisted head_source key ('nose' for legacy, 'skull' for new) so the restored face keypoints from Phase 1b cannot double-correct the head pitch, and re-check _JOINT_FROM_RIG[Joint.HEAD] = ('head','mid') — with a skull HEAD the correct read-back is the head bone's tail, and the audit measured that this choice is worth the same order as the fix itself. Carry proposal 0's build items: dropping the --feet guard at tools/fetch_weights.py:69 is mandatory, not optional (the repo ships no models/ directory, so a client build silently falls through to rtmlib's downloader — the exact failure detect/models.py exists to prevent), plus the Halpe checkpoint in pose3d/selftest.py's presence check, the PyInstaller spec and the Docker staging. Re-baseline every '% of body height' threshold in the same commit (height 0.1187 -> 0.1303 m, +9.8%; character scale 111.11 -> 110.09) — Phase 0's fixture makes this mechanical.

GRAFT 2 (from proposal 0, Phase 1 item 4; same idea in proposal 1, Phase B item 4) into the winner's Phase 1 — make the gap fill visible, not just counted. Add Frame.filled (bool, NUM_JOINTS) persisted by io_project alongside corrected, set by the single-frame interpolation, drawn hollow/amber in both the 2D and 3D views, and reported separately from the reconstructed-joint count. Today frames 0012 and 0021 report 15/15 while two joints are inventions; a symmetric midpoint is honest arithmetic but is still shown as if measured. The same array must feed blender_export._character_bone_frames so the export's hold-last-known rule in blender_job.py:454 becomes unreachable for short gaps and view and export stop disagreeing about what a dropout means.

GRAFT 3 (from proposal 0, Phase 2 item 4b and its named fallback) into the winner's Phase 2 — two additions to the roll, both cheap. (a) Resolve the captured bend normal's SIGN against the torso rather than leaving it to cross-product order: flip n so dot(n, Rz @ shoulder_line) > 0 for arms and dot(n, Rz @ hip_line) > 0 for legs. Anatomically the hinge normal lies along the limb's medial-lateral axis, so this is well defined AND stateless. (b) Write down the fallback the winner lacks, so the <= 15 deg continuity gate has somewhere to fail to: pose3d/geometry/retarget.py::roll_references(poses3d, valid) -> (T, B, 3), a pure function doing the hemisphere fix plus a symmetric 3-tap smoothing of the REFERENCE VECTORS across frames, called identically by view3d's per-take precompute and by blender_export._character_bone_frames and passed into pose_bone_matrices(..., roll_ref=...). Explicitly do NOT take proposal 1's per-bone RollState hysteresis: view3d.set_pose is driven per frame from main_window.py:450 in scrub order, so caller-owned temporal state desyncs the view from the export.

GRAFT 4 (from proposal 0, Phase 1 item 9; same in proposal 1, Phase B item 7) into the winner's Phase 1, before the smoother comes out — the noise-floor probe the audit never ran. Re-triangulate the take 20x under the bbox-jitter the detection auditor already characterised (sigma giving 1.26-2.79 px of 2D movement) and report per-joint 3D spread against the 9.36 mm median inter-frame motion. Under ~1 mm confirms that removing the smoother introduces no visible jitter on stop-motion; above ~3 mm, ship a zero-phase filter on at low alpha instead. This converts the winner's strongest assumption into a measurement, and it is the only defence against the one way this plan can look worse to the client while every number improves.

GRAFT 5 (from proposal 0, Phase 5 and proposal 1, Phase F) into the winner's Phase 5 — record gravity even though the manual control stays deferred. Add orient.up_candidates(rig, poses) returning the three proxies (cross of the camera x-axes; negated mean camera up; tag-row cross when a layout exists) with their pairwise disagreements, sign-fixed by the binary test that the take's mean NECK sits above its mean ANKLE (a sign test cannot leak body lean into the axis). Write world_up and world_up_source into extrinsics.json with the spread; main_window._apply_view_orientation and blender_export.py:59-61 use it when present and fall back to sequence_up when absent, so a legacy project's orientation is bit-for-bit unchanged. Replace the standing sidebar paragraph with the number and its uncertainty ('vertical: camera pair, +/-9 deg vs the tag row'). Carry proposal 1's correction into the docs: de_tilt_matrix is ONE constant rotation, so sequence_up removes only the MEAN lean — the per-frame lean (10.83 deg median) is preserved and must stay preserved. This is a day's work, changes nothing for old projects, and stops a ~15 deg residual tilt being invisible.

GRAFT 6 (from proposal 1, Phase A/E/F test design) into the winner's Phases 5 and any gravity work — the invariance assertion. Every robustness or frame-of-reference phase must assert that all Phase 0 shape metrics (bone CV, epipolar, symmetry, retarget %, height) are bit-identical on the client fixture before and after. A calibration-hardening or de-tilt phase that moves the reconstruction has done something else; this is the cheapest possible proof it did not silently re-tune.

GRAFT 7 (from proposal 0, Phase 6 item 3 and proposal 1, Phase A item 3) into the winner's Phase 4 — show BOTH reprojection numbers, not just the switch to fitted3d. Emit 'measured' (f.pose3d: do the two views agree?) and 'delivered' (f.fitted3d: what you are looking at) side by side, both normalised by that camera's figure height. After Phase 1 they should coincide (today 4.24x left / 6.17x right apart); their difference is the permanent regression detector for every later phase, and it is the readout that would have made the smoother visible at all.

GRAFT 8 (from proposal 0, Phase 4 item 3c/3d; also proposal 1, Phase H item 2/6) into the winner's Phase 3 — two cheap deliverable options the winner omits. (a) Expose the stepped/one-frame-per-pose switch and default BVH/FBX to one frame per pose: blender_job already supports schedule=None (blender_job.py:444-445), so this exposes a capability rather than adding one, and it makes export frame k+1 correspond to photograph k. (b) Add a fixed-camera mp4 rendered from the LEFT camera's own known pose alongside the turntable — a turntable spin over the whole clip is precisely what makes 'does the character follow the keypoints' unanswerable by eye, and the client's comparison is photo-to-frame.

GRAFT 9 (from proposal 0, Phase 7 item 2 and proposal 1, Phase D item 3) into the winner's Phase 5 — disclose the gate, not just the rejection. Report the chosen epipolar threshold and the observed Sampson distribution in the UI, and give rejected joints the tooltip 'rejected: the two views disagree by N px (gate M px)' next to the COL_PURPLE dot. A rejection the user can see the number for is a diagnosis; a purple dot alone is a new kind of silence.
CUTS: CUT 1 — 'DELETE smooth_temporal (lines 129-143) outright' (Phase 1). Delete the CALL and the smooth/alpha parameters and all three smooth=True call sites (import_dialog.py:214, model.py:55, tests/build_demo_project.py:77) — that is what buys the entire measured win — but keep the function, re-implemented zero-phase (forward EMA then the same EMA over the reversed sequence, NaN-aware), exposed as an import-dialog checkbox defaulted OFF and labelled 'video-rate capture only'. The plan's own client question 7 asks whether any future take is video-rate; removing the capability before that answer arrives makes the rollback a re-implementation instead of a checkbox, and 'turn smoothing back on' must remain a supported answer for a genuine video take. Keep the deletion of bonefit.py:140 (the NaN carry-forward) unconditionally — that line is a defect at every alpha.

CUT 2 — the wholesale Halpe-26 deferral in Phase 6. See graft 1. The reasons given for deferring (NECK/PELVIS redefinition, +9.8% height, the neck-Lshoulder regression, the _NOSE_PITCH collision) are all real, and they are all arguments against the MODEL SWAP, not against the head fix; proposal 1 shows the two are separable by a policy dict, and Phase 0's fixture is exactly the instrument that makes the re-baseline a mechanical single commit. Keep deferring native NECK/PELVIS and the foot keypoints — the neck-Lshoulder CV gate (must not exceed its 5.15% COCO baseline) is what decides those, and it fails today.

CUT 3 — 'delete focal_from_exif and tests/test_intrinsics_exif.py' (Phase 5). Keep both. Fix DigitalZoomRatio (tag 41988) so the function computes ~3570 px instead of 2833, record source='exif', and leave the default at f = max(w, h) with the comment the plan already writes (assumed (4080, 2048) is the best body-epipolar score of everything tried, 4.91 px, and all calibration error combined is bounded at 1.11 px). Deleting the function re-loses the specific fact that commit f4c92f9 reverted a number that was computed wrong rather than a number that was wrong in principle — which is precisely how the same revert gets re-litigated — and the plan's own client question 5 keeps an intrinsics path live. Invert the test's assertion rather than removing the file.

CUT 4 — the unconditional Phase 2 acceptance 'roll error <= 5 deg median on all eight limb bones'. Keep it for upper_arm.L/R and thigh.L/R, where the elbow and knee are hinges and the bend plane genuinely determines the parent bone's roll — those are true errors (53.5 / 13.6 / 21.4 / 27.1 deg medians today). For forearm.L/R and shin.L/R record it as a documented CONVENTION, not a measurement: no hand or foot keypoints exist, pronation was never observed, and F02's refuter explicitly flags those four as partly definitional. A future reviewer reading '0.0 deg' on a forearm must not read it as accuracy, and the four _BEND entries must stay independently removable if the client dislikes the twist.

CUT 5 — shipping the Phase 5 items as one 'latent robustness, changes nothing today' block. The phase already promises five separate commits; make that binding, and single out the IPPE scoring loop: it is the only change in the entire plan that can select a DIFFERENT calibration than today's, so its acceptance ('reproduces tag 14, frame 0001, branch 0 for both cameras to 0.000 deg / 0.000 mm on the fixture, and rejects tag 13 in 26 of 26 frames') must be a blocking test that lands in the same commit, not a phase-level claim. Everything else in Phase 5 can then be reverted independently without touching the rig.

CUT 6 — the 'either omit non-deform helper bones from pose_bone_matrices or pin them at rest' option in Phase 3. Pin them at rest; do not omit them. Omitting changes the exported armature topology, which blender_job's bone-name lookups, the FBX writer and the plan's own FK round-trip assertion all depend on, and a bone that disappears between versions is a structural surprise for any downstream retargeter. Pinning fixes the 42.5%-of-rig-height translation channel with no topology change and keeps the round-trip test comparing like with like.

## Judge 2: best_index=2
- P0: total 39 (acc 9, sound 8, risk/effort 5, fit 8, verif 9)
  + Widest coverage of the measured budget, ordered by what the client sees: smoother (98% of post-triangulation deviation), roll, dead head feature, root motion, saturated gauge. Uniquely treats delivery mechanics as first-class in Phase 1 (pipeline_version stamp + recompute-on-open with a numeric banner and Restore) — the audit never noticed that opening a project never recomputes, so without it the fixed build shows the client exactly what they complained about. Correctly catches that flipping pipeline.py:153 is a no-op (verified: import_dialog.py:214 and model.py:55 both pass smooth=True). Roll fix is STATELESS with an explicit ship gate (max consecutive-frame roll change <=15 deg) and a specified pure-function fallback (roll_references over the whole take) — the only design of the three that keeps pose_bone_matrices a pure function AND has a contingency, which is what preserves view/export parity. Honours every refutation explicitly: keeps the nose-pitch clamp for legacy (F03), refuses to force soles horizontal, refuses symmetrised targets, refuses the essential-matrix fallback, sells extrinsics/bundle work as insurance not accuracy. Phase 8 acceptance requires the new resolver to reproduce the shipped tag-14/frame-0001/branch-0 choice exactly. Phase 9 threshold discipline (1.5x today's value, never today's value) directly answers F32's flake warning. Acceptance numbers are concrete and traceable to the baseline throughout.
  - Largest surface by far: 9 phases at M,M,L,M,M,M,M,L,S for one developer — Halpe skeleton swap, calibration rewrite, gravity, export and CI all in one plan. Its own roll-conditioning weight contradicts its own argument: it correctly notes the left elbow's minimum captured bend is 22.7 deg and the normal jumps 55.7 deg there, then picks w = clip((sin b - sin 8)/(sin 25 - sin 8),0,1), which is ~0.87 at 22.7 deg — near-full correction exactly where the jump lives. Only the ship gate saves it. Phase 1 item 6 (freeze unobserved joints at x0 to keep method='lm') leaves the bone residuals that touch a frozen joint in the least-squares, so a joint frozen at the centroid pulls the observed joints — the hazard trf currently avoids; it never says to drop those residuals. Phase 4 spends real Blender-version budget on CONSTANT interpolation and a quaternion hemisphere pass; the delivered file (I checked) already holds each pose bit-identically for 21 frames, so that work is justified by an unverified concern. Phase 3's Halpe gate demands neck-Lshoulder CV <= the 5.15% COCO baseline and answers the measured 8.13% regression with 're-derive the clavicle rest reference' — a hand-wave where proposal 1 has a cleaner answer. Some acceptance targets are aspirational rather than measured (Panoptic reference 'reads green'). Adds the most new machinery (persisted smoothing mode + UI, foot2d/foot3d arrays, retarget.py module).
- P1: total 36 (acc 8, sound 7, risk/effort 4, fit 8, verif 9)
  + The single best architectural idea in the set: one Qt-free take_quality() with three consumers (pytest, CLI, sidebar), so the client's readout and the ship gates are literally the same code — that collapses F07/F24/F25/F32 into one module and makes every later phase auditable. Sharpest analysis of F04 anywhere in the set: a head/neck/pelvis POLICY DICT that takes Halpe's native skull HEAD (12.65 -> 3.93% of height, neck-head CV 9.53 -> 3.85%) while KEEPING NECK/PELVIS as 2D midpoints, because the measured regression (neck-Lshoulder CV 5.15 -> 8.13%) comes from the native neck, not the model — it wins the head without the cost, where the others either gate the whole swap or defer it. Catches the trap that 'reject only if BOTH per-image distances exceed' is strictly MORE permissive than today (dR max 33.8 px vs a 35.8 px allowance) — a subtle inversion the other plans do not flag. Phase E's tag admission by planarity residual (tag 13 at 2.41 px vs 0.21-0.84) plus JOINT (left,right) branch scoring is precisely right on F22/F23, and it correctly frames the bundle as provenance-only. Gates are fixed BEFORE the run, and several phases use invariance as their own self-test (a calibration or gravity phase that moves the shape has done something wrong). Verified demo.bvh's null-root/14-sibling fallback structure — I confirmed it.
  - Phase G's roll carries per-bone hysteresis in a 'RollState object the CALLER owns', asserting both consumers 'iterate the take in order'. That is false for the view: main_window.py:450 calls view3d.set_pose(f.fitted3d, ...) on every frame change, i.e. random access as the user scrubs — so the view's roll would depend on navigation order and diverge from the export, breaking the one hard constraint (pose_bone_matrices is the single source of truth). Its own bit-for-bit parity test would fail, and this sits in the phase that produces the client-visible change, ranked last. Phase A is L effort and ships zero accuracy; the smoother fix (the 98% item) lands second and the retarget last, so the client waits longest for anything visible — a real delivery risk for one developer against an unhappy client. Factually wrong about the deliverable: '~93% of the delivered animation is Blender's Bezier interpolation' — I opened assets/Imported_Session.bvh and each pose's 21-frame hold is bit-identical, so ~70% of frames are exact copies, not interpolation. Phase C re-opens the _JOINT_FROM_RIG[HEAD] readback and cites the 1.72% 'aim straight at the point' variant, which the F03 refutation measured as TRIPLING the visible nose-to-mesh distance (1.56 -> 4.34 mm); it defers to the harness, but the harness metric is exactly the one the refuter showed is misleading here. Shares proposal 0's freeze-at-x0 hazard in fit_bone_lengths. Eight phases with A, E and H all at L.
- P2: total 44 (acc 8, sound 9, risk/effort 9, fit 9, verif 9)
  + The only plan that closed the audit's own admitted gap by opening the file the client watched, and I re-verified every claim: assets/Imported_Session.bvh declares Frames: 772 at 30 fps for 26 poses; the hips translation channels are constant at (0.000000, -0.131342, 2.851750) across all 771 motion rows — exactly zero root travel against 116% of body height of real pelvis motion; each pose's hold is bit-identical for 21 frames before the ease begins; and shin.L.001/shin.R.001 exist as top-level siblings of hips (orphan IK helpers adopted by _eparent) carrying translation channels. That measurement RETIRES work: it shows the quaternion-hemisphere / CONSTANT-interpolation rework both other plans budget for is unnecessary (max overshoot 3.80 deg), and it promotes two defects nobody else found. Ordered by measured effect per line changed, and three of six phases are net deletions. Phase 1 (S) deletes smooth_temporal and the NaN carry-forward and, by making the drag path and batch path the same computation, dissolves F10 with no replacement code; the symmetric single-frame gap fill is the honest replacement for the one thing the smoother was doing. Phase 1b is the leanest correct delivery mechanic (version stamp, recompute-on-open, non-destructive detect_project, and a 'Re-detect face points only' action that is the migration path for the client's existing headless project). Phase 2's roll is stateless AND its confidence weight (sin 20 -> sin 40 deg) is the only one calibrated to actually suppress the measured 55.1 deg jump at the left elbow's 22.7 deg minimum bend. Rigorously honest about what it does NOT claim: refuses to assert foot tilt improves (measured L 38.5 -> 39.6), refuses to force soles horizontal, refuses to touch _NOSE_PITCH (the legacy path), refuses to change the 71.5 px fraction without a distribution, enumerates all five refuted findings as do-not-reopen. Phase 0 sets fixture thresholds 10-15% ABOVE today's values with three xfail-strict gates that fail today — exactly the discipline F32 asked for. Phase 6 is an explicit deferral register with the gates that would unblock each, written as a skipped test body rather than prose.
  - Defers Halpe-26 wholesale, and with it the largest single remaining measured win: HEAD retarget 12.65 -> 3.93% of body height and neck-head bone CV 9.53 -> 3.85% (the worst bone in the entire baseline). Its stated reason — that native NECK/PELVIS redefine the skeleton and regress neck-Lshoulder CV 5.15 -> 8.13% — is real but proposal 1 shows it is avoidable by taking the native HEAD only; the deferral is therefore stronger than the evidence requires. Defers gravity (F15) entirely, so the delivered animation keeps standing ~15 deg off the three-proxy consensus; the provenance half (record world_up and its source in extrinsics.json, fall back to sequence_up) is cheap and needs no new UI, so the all-or-nothing deferral overshoots. Ships keep_root_motion defaulting OFF for BVH/FBX — the most conservative possible choice on the one deliverable-facing defect it uniquely verified; the client's next export still animates in place unless they find the flag. Leaves the view-vs-export placement disagreement (25.5% of rig height) unfixed, since it grounds the view on the ankle but changes nothing on the export side. Deletes focal_from_exif and its test outright, discarding the audit's own correction (2833 px was 21% short because DigitalZoomRatio was ignored; true f ~3570) and the future EXIF/checkerboard ladder. The Phase 0 xfail-strict on helper-bone translation lives in a needs_blender-gated test that skips locally, so that particular tripwire never fires on the developer's machine. Phase 5 is L and bundles five independent changes (mitigated by its own five-commit instruction).
GRAFTS: 1. NATIVE SKULL HEAD WITHOUT THE SKELETON SWAP — from proposal 1's Phase C, into the winner's Phase 3 (as a new item, gated by the Halpe checklist the winner already wrote in Phase 6). Adopt proposal 1's policy dict: skeleton.map_halpe26(kp, scores, head='native', neck='derived', pelvis='derived'). Take Halpe's index-17 skull vertex as HEAD (retarget 12.65 -> 3.93% of body height with no face points, 9.09 -> 2.92% with them; neck-head bone CV 9.53 -> 3.85%, removing the worst bone in the baseline) and KEEP NECK/PELVIS as the 2D midpoints, which is exactly what avoids the 5.15 -> 8.13% neck-Lshoulder regression the winner cites as its reason to defer. Mandatory companions: drop the --feet guard at tools/fetch_weights.py:69 and stage the checkpoint in the PyInstaller spec, Docker and selftest (or the shipped bundle silently falls through to rtmlib's downloader); add a per-project keypoint_model/head_source key so _NOSE_PITCH and its clamp stay live for legacy nose-HEAD projects and are bypassed for skull-HEAD ones; re-baseline the fixture in the same commit (body height 0.1187 -> 0.1303 m, +9.8%, invalidates every % -of-height threshold). Do NOT graft proposal 1's re-opening of the _JOINT_FROM_RIG[HEAD] readback against the 1.72% metric — F03's refuter measured that variant tripling the visible nose-to-mesh distance (1.56 -> 4.34 mm).

2. RECORDED WORLD UP, PROVENANCE HALF ONLY — from proposal 0's Phase 5 and proposal 1's Phase F, into the winner's Phase 5 (latent robustness). Add estimate_world_up(rig, tag_layout=None) returning the three proxies (cross of the camera x-axes; mean camera up; tag-row cross normal) with their pairwise disagreement, sign-fixed by requiring the take's mean NECK above its mean ANKLE; write world_up + world_up_source into extrinsics.json; have main_window._apply_view_orientation and blender_export.py:59-61 read rig.world_up when present and fall back to orient.sequence_up when absent, so legacy projects are bit-for-bit unchanged. This is provenance plus one sidebar number, not the new UI the winner defers, and it addresses the ~15 deg tilt the winner otherwise leaves entirely. Keep the manual 'level the figure' control deferred, and keep proposal 1's correct note that sequence_up removes only the MEAN lean (per-frame lean 10.83 deg median is preserved to 1.5e-11) so nobody 'fixes' a non-defect.

3. ROOT MOTION ON BY DEFAULT FOR BVH/FBX — from proposal 0's Phase 4 and proposal 1's Phase H, into the winner's Phase 3. The winner verified the exported hips translation is exactly constant while the pelvis travels 0.1362 m (116% of body height, monotone in x, present in the raw triangulation); shipping that fix default-OFF means the deliverable does not change. Default ON for BVH/FBX, OFF for the turntable mp4, per-export overridable, with the same offset applied in blender_job.py:524's fallback path so no export path disagrees with another.

4. TWO GAUGE NUMBERS, NOT ONE — from proposal 0's Phase 6 item 3 and proposal 1's Phase A item 3, into the winner's Phase 4. Show 'measured' (reprojection of f.pose3d) alongside 'delivered' (reprojection of f.fitted3d), both normalised by that camera's figure height. Their DIFFERENCE is the actionable signal and is the regression detector for every later phase; the winner switches the gauge to fitted3d but loses that difference. After Phase 1 the two should coincide — if they ever diverge again, something downstream of triangulation is moving the pose.

5. THE 'BOTH EXCEED' TRAP — from proposal 1's Phase D item 4, into the winner's Phase 5. When re-expressing the epipolar gate as a per-image point-to-line distance against each image's own diagonal, reject when EITHER view exceeds, never when both do: dR is systematically smaller (max 33.8 px against a 35.8 px allowance), so the 'both' form measures strictly more permissive than the status quo and inverts the intent. The winner proposes the per-image gate without this caveat.

6. ANTIPODAL DETERMINISM TEST — from proposal 0's Phase 2 item 7 and proposal 1's Phase G item 8, into the winner's Phase 2 tests. The explicit roll reference subsumes F43, but add the regression test at 179/180/181 deg asserting the roll changes by < 5 deg (today a 1 deg wobble at exactly 180 flips it 163.8 deg). The winner fixes the defect and never pins it.

7. NOISE-FLOOR PROBE BEFORE MERGE — from proposal 0's Phase 1 item 9 and proposal 1's Phase B item 7, into the winner's Phase 1. Re-triangulate the take ~20x under the bbox-jitter sigma the detection auditor characterised (1.26-2.79 px of keypoint movement) and report the per-joint 3D spread against the 9.36 mm median inter-frame motion. The winner correctly says the take contains no repeatability data and then deletes the smoother anyway; this converts that assertion into a number and pre-answers the client if they call the restored 17% of inter-frame motion 'jitter'.

8. DRAW THE GAPS AND THE REJECTIONS — from proposal 0's Phase 1 item 4 / Phase 2 item 8 and proposal 1's Phase B item 4, into the winner's Phase 1 (fill) and Phase 5 (rejection mask). The winner reports gaps in FitReport and rejections in a mask but never renders them; a filled joint must draw hollow and a cross-view-rejected joint must draw in its own colour with the tooltip naming the disagreement, or the user still cannot tell a rejection from a detection failure.

9. calibration/report.json — from proposal 0's Phase 8 item 3 and proposal 1's Phase E item 3, into the winner's Phase 5. The winner writes provenance into extrinsics.json; add the separate report carrying per-tag IPPE rms, admission/rejection reason per tag, branch ratios per camera, observation counts, marker_length and focal source, and assert it is sufficient to rebuild the rig bit-for-bit. That is what makes the NEXT take diagnosable rather than brute-forceable.

10. THE FAILURE MESSAGE THAT NAMES THE CAMERA — from proposal 0's Phase 7 item 6 and proposal 1's Phase E item 9, into the winner's Phase 5. 'left camera saw tags 13, 14, 17 in 26/26 frames; right saw tag 14 in 11/26 and nothing in 15 — move the right camera so at least two tags are in frame.' Both other plans have it; the winner has the diagnosis in its client questions but not in the product.
CUTS: 1. DROP the outright deletion of focal_from_exif and tests/test_intrinsics_exif.py (Phase 5). The function is caller-less and its number is wrong, but the reason it is wrong is now known and measured: it ignores DigitalZoomRatio, so its 2833 px is 21% SHORT of the true ~3450-3600 px, which means commit f4c92f9 reverted a number computed wrong rather than an approach that is wrong. Deleting the code and its test throws away the record of that and the only EXIF rung of a future focal ladder. Instead: fix the zoom factor, leave it with no automatic caller, refuse it for a camera with no EXIF (the right camera has none), and keep the test — rewritten to assert the corrected value and the reason the assumed focal remains the default (it scores 4.91 px body epipolar, the best of everything the audit tried).

2. DROP the xfail(strict=True) on the helper-bone translation channel from Phase 0's test_export_smoke.py. Every export test is needs_blender-gated and skips locally, so an xfail-strict there never fires on the developer's machine and gives false assurance. The winner already has the evidence without Blender — assert it against the committed BVH facts (or a small parsed fixture) in Phase 0, and make it a plain assertion in Phase 3 where the fix lands.

3. DROP the <name>_frames.json frame map as a default-path artefact (Phase 3). Once one-frame-per-pose becomes the default for BVH/FBX (graft 3's sibling change, which the winner already offers as a switch), export frame k+1 IS photograph k and the map is redundant. Keep it only for the stepped/mp4 path, where the 1+30k offset is real.

4. DROP the unquantified hand claim in Phase 2 ('hands keep the forearm's matrix, whose roll is now defined instead of arbitrary'). It is true but the plan sets no number for it while setting numbers for everything else. Either adopt proposal 0's acceptance (hand.L posed-axis spread over the take 125.9 deg -> <= 40 deg) or delete the sentence — the plan's own standard is that a claim without a gate does not ship.

5. DROP the blanket deferral of gravity from Phase 6 (superseded by graft 2). Keep deferring only the manual level control; the recorded-up provenance is a Phase 5 item.

6. DROP the blanket deferral of Halpe-26 from Phase 6 (superseded by graft 1). Keep deferring the native NECK/PELVIS and the foot keypoints — those are the parts that redefine the skeleton and invalidate the thresholds — not the native skull HEAD.

7. SOFTEN the outright deletion of smooth_temporal in Phase 1: delete the causal EMA, but land the zero-phase (forward-backward, NaN-aware) replacement behind an import-dialog checkbox defaulted OFF in the same commit, as proposals 0 and 1 both do. The winner's own client question 7 ('stop-motion or video-rate?') is unanswered, and re-adding a filter later under client pressure is exactly when a causal one gets restored by accident. Ten lines now buys the supported answer for a genuine video take.